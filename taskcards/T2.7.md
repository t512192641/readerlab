# T2.7 有边界知识框架 Prompt 对照实验

> 本卡只组织一次低成本 Prompt 对照。材料、三个学科、调用数、模型、路径和停止位置仅对 `T2.7-BOUNDED-FRAMEWORK-PILOT-01` 有效，终局后自动失效。

current-state: COMPLETE
experiment-id: T2.7-BOUNDED-FRAMEWORK-PILOT-01
review-mode: transparent

## 1. 任务编号与标题

`T2.7`：有边界知识框架 Prompt 对照实验。

## 2. 目标

只回答：在保持 T2.6 的材料、学科标签、模型和隔离方式不变时，把 Expert 的交付对象从宽泛“知识增量”收紧为“一套可由读者重建的、有明确边界的知识框架”，能否产生产品负责人需要的内容。

本轮不生产画像、知识背景包、理论清单或自动选角，不联网，不读取旧 run，也不进入来源研究、完整专家稿、知识卡、Writer、Assembler、Judge、M2 或 T3。

## 3. 允许读取清单

### 材料与范围

- 输入文件：`materials/T2.4-IDEA-PILOT-01/source.epub`；大小 `672638` bytes；SHA-256 `3baf9932c92412f0e7d0ccae993182f55f62e2fab5ec46a3882d675476288664`。
- 只读 `text/part0019.html` 从首个 `<h2 class="calibre15"` 起至 `<ol class="duokan-footnote-content1">` 起始标签前的连续片段，即第 13 章标题与 18 个正文段落。
- member：`15941` bytes，SHA-256 `c72b2e794266b7eb0d2589045328ea98c9511d8a56aa4037d924e036d1fac891`。
- scope：`13337` bytes，SHA-256 `216f3a01176e10084ee7a011da7f85cd8bb6e6f1558546db6414f9a285b05dad`。
- 排除脚注、其他 member、网络、`GOLD-STANDARDS.md`、`examples/`、旧 ReaderLab、T2.4—T2.6 run、判词与生产答案。身份或范围冲突立即 `STOPPED`。

### v2 Expert Prompt

三个 Expert 使用 `runs/T2.7-BOUNDED-FRAMEWORK-PILOT-01/inputs/expert-prompt-v2.md` 中完全相同的任务；只替换【学科】标签。

冻结 Prompt SHA-256：`e2ce8df8dbb71590e60320bafdb40bd29322aafbd7d1e192b7d9e496b6b505b8`。任何字节变化都必须停止，不得在本轮追加指令或补丁。

```text
你是以【学科】为知识来源的阅读 Expert。学科标签只规定你可以调用的知识范围，不要求扮演人物或采用特定文风。

完整阅读本章。只在原文自然触发了一套值得读者学习、且有明确边界的成熟知识框架时输出。这里的“框架”不要求拥有著名名称，但读者读完后必须能够重建：它解决什么核心问题；有哪些不可缺少的组成部分；这些部分如何相互作用；它在什么条件下成立或失效；以及如何把它用于观察另一个问题。

如果存在这样的真实连接，请从框架自身的问题和整体结构开始，把它作为一个完整知识对象讲清楚，再用原文作为实质案例帮助理解，最后说明它如何改变对原文的认识。单个概念、一段历史解释、一条因果机制，或若干未被组织成整体的专业观点，都不构成完成。

正文应自然、连贯、面向普通成年读者，不必按上述问题设置栏目或展示检查表。没有合适的完整框架时，只提交 silent。
```

机械输出为 JSON：有内容时只含 `status: submit`、`title`、`anchor`、`lesson`；沉默时只含 `status: silent`。字段不是写作提纲，不规定栏目、字数、概念数或段落数。

### 隔离调用

所有调用均使用 `fork_turns="none"`，只能读取冻结 EPUB、自己的学科标签和 v2 Prompt，不能读取本会话、owner、任务卡、其他 Expert、旧 run 或产品判词：

- `E01`：社会学；只能写 `runs/T2.7-BOUNDED-FRAMEWORK-PILOT-01/raw/experts/E01.json`。
- `E02`：历史学；只能写 `runs/T2.7-BOUNDED-FRAMEWORK-PILOT-01/raw/experts/E02.json`。
- `E03`：制度经济学；只能写 `runs/T2.7-BOUNDED-FRAMEWORK-PILOT-01/raw/experts/E03.json`。

不返工、不补调。总控不修改、筛选或拼接正文；只做机械验证并将原始输出逐字排版为标明学科的直接阅读页。

## 4. 交付文件清单

本轮 run 只允许以下 6 个文件：

- `runs/T2.7-BOUNDED-FRAMEWORK-PILOT-01/inputs/expert-prompt-v2.md`
- `runs/T2.7-BOUNDED-FRAMEWORK-PILOT-01/raw/experts/E01.json`
- `runs/T2.7-BOUNDED-FRAMEWORK-PILOT-01/raw/experts/E02.json`
- `runs/T2.7-BOUNDED-FRAMEWORK-PILOT-01/raw/experts/E03.json`
- `runs/T2.7-BOUNDED-FRAMEWORK-PILOT-01/review/expert-demos.html`
- `runs/T2.7-BOUNDED-FRAMEWORK-PILOT-01/acceptance/product-verdicts.md`

`READY_FOR_DISPATCH` 时 run 根不存在；`IN_PROGRESS` 后才能创建。`AWAITING_PRODUCT_VERDICT` 必须已有除产品判词外的 5 个文件；`COMPLETE` 必须 6 个文件全部存在。

产品判词文件必须记录产品负责人的直接判词，并且恰好各有一行 `verdict: ACCEPTED|REJECTED`、与之相符的 `product-accepted: yes|no` 和非空 `owner-quote: ...`；技术验证或模型意见不得代填。

## 5. 硬约束

- Expert 调用上限 3 次；当前高能力档；每位最多提交一份并允许沉默。
- 唯一方向变量是 v2 Prompt 对“完整框架”的正向完成定义；不得改变材料、学科或模型来救结果。
- 禁止返工、补调、研究和任何后续生产角色。
- 产品负责人直接阅读标明学科的页面，判断是否真正学会一套完整框架、连接是否真实、能否迁移；判词后停止。

## 6. 完成判据

- `implemented`：任务卡、v2 Prompt 和固定闭集存在。
- `integrated`：README、路线图和验证器指向真实状态。
- `verified`：`python3 validate.py --check-t27`、`python3 validate.py --write-manifest`、`python3 validate.py` 与 `git diff --check` 通过；阅读页可见正文与 raw 精确一致。
- `accepted`：只能由产品负责人判词；真实运行前为 `unknown`。

产品负责人已明确同意推荐方案并要求测试一轮，因此准备验证通过后可直接进入 `IN_PROGRESS`，无需再次请求同一授权。
