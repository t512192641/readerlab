# T2.8 既存知识对象核心卡实验

> 本卡只组织一次低成本 Prompt 实验。材料、三个学科、调用数、输出形态、模型、路径和停止位置仅对 `T2.8-KNOWLEDGE-OBJECT-PILOT-01` 有效，终局后自动失效。

current-state: COMPLETE
experiment-id: T2.8-KNOWLEDGE-OBJECT-PILOT-01
review-mode: transparent

## 1. 任务编号与标题

`T2.8`：既存知识对象核心卡实验。

## 2. 目标

只回答：在保持 T2.7 的材料、学科标签和控制器派发配置不变时，删除诱导 Expert 现场拼装“完整框架”的五项完成清单，改为寻找一个在本次阅读前已经存在、可辨认来源且具有稳定内核的知识对象，并只交付它的简短核心，能否更接近产品负责人需要的“把书读厚”。后端精确模型身份和实际 read-set 均不可独立核对，保持 `unknown`，因此本轮不宣称已证明精确模型相同或读取隔离实际发生。

本轮同时落实产品负责人要求的简短交付形态，因此不能单独归因“知识对象定义”和“篇幅压缩”各自贡献了多少。它不生产画像、知识背景包、理论清单或自动选角，不联网，不读取旧 run，也不进入来源研究、完整专家稿、知识卡、Writer、Assembler、Judge、M2 或 T3。

## 3. 允许读取清单

### 材料与范围

- 输入文件：`materials/T2.4-IDEA-PILOT-01/source.epub`；大小 `672638` bytes；SHA-256 `3baf9932c92412f0e7d0ccae993182f55f62e2fab5ec46a3882d675476288664`。
- 只读 `text/part0019.html` 从首个 `<h2 class="calibre15"` 起至 `<ol class="duokan-footnote-content1">` 起始标签前的连续片段，即第 13 章标题与 18 个正文段落。
- member：`15941` bytes，SHA-256 `c72b2e794266b7eb0d2589045328ea98c9511d8a56aa4037d924e036d1fac891`。
- scope：`13337` bytes，SHA-256 `216f3a01176e10084ee7a011da7f85cd8bb6e6f1558546db6414f9a285b05dad`。
- 排除脚注、其他 member、网络、`GOLD-STANDARDS.md`、`examples/`、旧 ReaderLab、T2.4—T2.7 run、判词与生产答案。身份或范围冲突立即 `STOPPED`。

### v3 Expert Prompt

三个 Expert 使用 `runs/T2.8-KNOWLEDGE-OBJECT-PILOT-01/inputs/expert-prompt-v3.md` 中完全相同的任务；只替换【学科】标签。派发前必须将本节正文逐字写入该文件并登记 SHA-256；不得在本轮追加指令或补丁。

冻结 Prompt SHA-256：`199b899037ca26d89f5e9d952e749a526e3bb050d232ee7dc0a4299185b0a1bf`。

```text
你是以【学科】为知识来源的阅读 Expert。学科标签只规定你可以调用的知识范围，不要求扮演人物或采用特定文风。

完整阅读本章。从本学科已有知识中，只寻找一个在本次阅读之前已经存在、具有可辨认来源和稳定内核的知识对象。它可以是理论、模型、命名概念、研究方法或思想工具，不要求著名、学界公认或体系庞大。

只有当这个知识对象的核心关系与原文存在实质连接，并能带来原文自身不能直接推出的新认识时才提交。名称、词语或表面主题擦边，针对本章临时归纳的一二三四结构，以及换到多数材料上仍成立的通用分析清单，都不构成连接。无法有把握地辨认它在本次阅读前的身份时，请保持沉默。

不要把它扩写成完整课程、辩论或叙事文章。只交付一张简短知识核心卡：用最少但足以理解的文字写清它是什么及其来源身份、稳定内核、为什么与原文真正相连，以及它新增了什么认识；只有理解所必需时才补一句边界。通常以一屏可读、约 300—500 个中文字为目标，不按字数机械截断。

没有合适对象时，只提交 silent。
```

机械输出为 JSON：有内容时只含 `status: submit`、`object`、`identity`、`core`、`connection`；沉默时只含 `status: silent`。字段只承载核心卡，不是要求扩写的栏目。`identity` 是模型本轮根据既有知识给出的来源身份，未经来源研究核验，阅读页必须明确提示这一点。

### 隔离调用

所有调用均使用 `fork_turns="none"`，只能读取冻结 EPUB、自己的学科标签和 v3 Prompt，不能读取本会话、owner、任务卡、其他 Expert、旧 run 或产品判词：

- `E01`：社会学；只能写 `runs/T2.8-KNOWLEDGE-OBJECT-PILOT-01/raw/experts/E01.json`。
- `E02`：历史学；只能写 `runs/T2.8-KNOWLEDGE-OBJECT-PILOT-01/raw/experts/E02.json`。
- `E03`：制度经济学；只能写 `runs/T2.8-KNOWLEDGE-OBJECT-PILOT-01/raw/experts/E03.json`。

不返工、不补调。总控不修改、筛选或拼接语义内容；只做机械验证并将原始输出逐字排版为标明学科的直接阅读页。

三次调用沿用 T2.7 的控制器派发配置：`agent_type: worker`、`reasoning_effort: high`、不指定 model override，由同一控制器继承默认模型。运行环境不暴露可持久核对的后端精确模型标识，因此“精确型号相同”为 `unknown`，本轮只能证明派发配置相同。读取白名单是派发指令，不是可独立观测的 read-set 证明；这些证据边界必须写入 `receipts/dispatch.json`。

## 4. 交付文件清单

本轮 run 只允许以下 7 个文件：

- `runs/T2.8-KNOWLEDGE-OBJECT-PILOT-01/inputs/expert-prompt-v3.md`
- `runs/T2.8-KNOWLEDGE-OBJECT-PILOT-01/raw/experts/E01.json`
- `runs/T2.8-KNOWLEDGE-OBJECT-PILOT-01/raw/experts/E02.json`
- `runs/T2.8-KNOWLEDGE-OBJECT-PILOT-01/raw/experts/E03.json`
- `runs/T2.8-KNOWLEDGE-OBJECT-PILOT-01/receipts/dispatch.json`
- `runs/T2.8-KNOWLEDGE-OBJECT-PILOT-01/review/knowledge-object-cards.html`
- `runs/T2.8-KNOWLEDGE-OBJECT-PILOT-01/acceptance/product-verdicts.md`

`READY_FOR_DISPATCH` 时 run 根不存在；`IN_PROGRESS` 后才能创建。`AWAITING_PRODUCT_VERDICT` 必须已有除产品判词外的 6 个文件，且判词文件不得提前存在；`COMPLETE` 必须 7 个文件全部存在。

若某位 Expert 合法提交 `silent`，阅读页必须显示其学科与 `silent` 状态，不得伪造空白知识卡；其余提交卡仍与 raw 逐字一致。

产品判词文件必须恰好各有一行 `verdict: ACCEPTED|REJECTED`、与之相符的 `product-accepted: yes|no` 和非空 `owner-quote: ...`；技术验证或模型意见不得代填。

## 5. 硬约束

- Expert 调用上限 3 次；控制器沿用 T2.7 的高推理配置，但精确后端模型身份不可核对；每位最多提交一个知识对象并允许沉默。
- 本轮改变的是错误的 Expert 完成方向和产品负责人要求的核心卡交付形态；不把它伪装成只改变一个变量的严格因果对照。
- “阅读前已经存在”约束对象的身份，不要求学术正统或统一名称，也不排除类似“血酬定律”的个人原创概念。
- 300—500 字只是本实验的软目标，不进入长期产品 owner，不得机械截断合格内容。
- 禁止返工、补调、研究和任何后续生产角色。
- 产品负责人直接阅读标明学科的页面，判断对象是否真实存在、连接是否实质、核心是否足够有用且交付是否足够精炼；判词后停止。

## 6. 完成判据

- `implemented`：任务卡、v3 Prompt 和固定闭集存在。
- `integrated`：README、路线图和验证器指向真实状态。
- `verified`：`python3 validate.py --check-t28`、`python3 validate.py --write-manifest`、`python3 validate.py` 与 `git diff --check` 通过；阅读页可见核心卡与 raw 精确一致。
- `accepted`：只能由产品负责人判词；判词前为 `unknown`。

产品负责人已明确同意该修改并要求继续推进，因此准备验证通过后可直接进入 `IN_PROGRESS`，无需再次请求同一授权。
