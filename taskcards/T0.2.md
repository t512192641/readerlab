# T0.2 轻量运行脚本

## 1. 任务编号与标题

`T0.2`：轻量运行脚本 `tools/run.py`。

## 2. 目标

以标准库实现 run 创建、冻结与检查的最小确定性骨架。

前置：T0.1 已通过；T0.3 只能在本任务提交后派发，以避免并发修改 `validate.py` 与 manifest。

## 3. 允许读取清单

- `AGENTS.md`
- `PRODUCT-DECISIONS.md`
- `ENGINEERING-LESSONS.md`
- `blueprints/PIPELINE-MAP.md`
- `blueprints/EXECUTION-ROADMAP.md`
- `taskcards/T0.2.md`
- `validate.py`

## 4. 交付文件清单

- 创建 `tools/run.py`
- 修改 `validate.py`
- 仅通过验证器重建 `audit/manifest.json`

## 5. 硬约束

- 依据路线图“总体架构：轻骨架、合同与执行 Agent”与 T0.2：只做 `new`、`freeze`、`check`，纯标准库、单文件；不得实现状态机、数据库或调度器。
- 依据 `ENGINEERING-LESSONS.md` 经验二：材料身份、目标不存在、目录隔离、preflight／postflight 与冻结必须可确定性检查。
- 依据 `PRODUCT-DECISIONS.md`“材料进入与范围冻结”：来源或范围未确认必须阻塞，不得猜测；生产路径禁读金标与样张。

## 6. 完成判据

机械：`python3 validate.py`、`python3 -c "import ast, pathlib; ast.parse(pathlib.Path('tools/run.py').read_text(encoding='utf-8'))"` 和任务内的临时目录 smoke check 均退出 0，且仓库内无 `__pycache__` 或 `.pyc`。主控：确认 run 路径不可覆盖、冻结后改动可发现、检查不替代语义或产品验收。
