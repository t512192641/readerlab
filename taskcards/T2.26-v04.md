# T2.26 未动章节迁移与影子 Reader Quality Rater（v04）

> 本卡沿用 T2.26 已确认的完整生产基线：在一章完整作者原文中，以单 Expert 自主发现、独立比较、候选池广度旁路、同源展开、独立来源审核、Writer v1.3、Fidelity、完整章节装配、P1/P2/P3 人工门和影子 Reader Quality Rater 验证前瞻迁移。影子 Rater 只产生隔离研究信号，绝不取代产品判断或进入生产门。

> v01、v02、v03 及全部既有 run 原样保留。本轮不读取或复用 v02/v03 的语义产物。第 7 章已被 v02 语义角色读取，必须排除；第 2 章在 v03 只经历机械控制操作，未被语义角色读取，仍是未动章节，明确不加入排除集。

current-state: COMPLETE
scope: run-only
experiment-id: T2.26-PROSPECTIVE-TRANSFER-AND-SHADOW-RATER-04
active-version: v04
card-status: FROZEN
starting-branch: codex/readerlab-book-lens-original-goal-v1
starting-head: d63118fd057e0b9a8aadc05b2fae244035c02891
model-target: gpt-5.6-terra
reasoning-target: medium
scheme-product-status: ACCEPTED
run-start: 2026-07-23T15:55:42Z
run-close: 2026-07-23T15:59:54Z
terminal-a1: NOT_REACHED_INVALID_RUN
terminal-a2: INVALID_RUN
terminal-rater-procedure: NOT_REACHED_INVALID_RUN
terminal-rater-signal: NOT_REACHED_INVALID_RUN
terminal-patch: PATCH_NOT_RUN
deterministic-verification: FAIL
product-status: NOT_REQUESTED

## 0. v04 启动硬门：控制合同预检

开始材料选择或任何语义调用前，必须以 `scripts/t226_control_contract_preflight.py` 在本 run 新目录中机械生成、二进制回读并验证以下五份控制合同及 `contract-manifest.md`：

- `control/writer-contract-v1.3.md`
- `control/target-reader-contract-v01.md`
- `control/reader-quality-rubric-v01.md`
- `control/human-local-review-form-v01.md`
- `control/issue-schema-v01.json`

预检必须同时通过逐文件结构、Reader 六题与 issue 字段交叉一致、四个单篇终局一致、完整章节组合区独立、target-reader 禁止过程／历史文本、manifest 实际 SHA 一致、冻结后不可变性，以及 `artifact-registry.jsonl` 对所有既有产物的闭集覆盖与唯一 self-exemption。只有输出明确为 `status: PASS`、`chapter_body_reads: 0`、`semantic_model_calls: 0` 后才可进入第 3 节。预检器唯一允许读取的非控制输入是 `docs/writer-style-guide.md` 的确定 byte slice；不得读取 EPUB 或章节正文。

Writer v1.3 必须是 `docs/writer-style-guide.md` byte 0 至唯一 `## 三、A/B 实验指令` 前的原始 bytes，精确为 6003 bytes、SHA-256 `9a7a3bcaaf4d81cdc2936a2217ea9a147ea91b2edccf5ee0d83fd06a60462ffe`，不得包含 A/B 样本或增加新规则。

合同和本任务卡从 run-start 起不可改写；任何 hash、manifest 或冻结合同变化均为 `INVALID_RUN`。本卡的唯一允许关闭回填字段是 run-close、五个 terminal、deterministic-verification 与 product-status。

## 1. 事实 owner、组织与通用隔离

`PRODUCT-DECISIONS.md`、`ENGINEERING-LESSONS.md`、`blueprints/PIPELINE-MAP.md` 是长期事实 owner；本卡只冻结本次实验参数。生产端不得读取 `GOLD-STANDARDS.md`、`examples/`、历史答案、其它 run 语义产物、产品判词理由或本卡未逐项授权的材料。

总控只管理一名全新 `gpt-5.6-terra / medium` 生产小组长，接收 P1、P2、P3、揭盲匹配和可选 C 的路径／SHA／状态／成本摘要，不写候选、课程、来源结论、Reader 或 Rater 判断。小组长管理所有组员、逐次核验 read-set、模型、reasoning、输出路径与冻结身份，并维护 `control/production-group-ledger.jsonl`、`control/usage-ledger.jsonl` 与 registry；语义交接只能经过本卡指定的冻结文件。

语义 artifact 一经写入即冻结。仅在角色读材料前发生的纯技术失败可用完全相同输入重试一次；语义失败不得重跑、换模型或提高 reasoning。小组长可按本卡停止对象，但无 P1/P2/P3/Rater 匹配或产品接受权。

## 2. 固定研究问题、终局与非目标

**A1** 记录 Writer v1.3 首稿迁移；**A2** 记录完整冻结流程是否形成经 P3 接受的合法产物，A2 不得覆盖 A1。**B** 只测试影子 Rater 在同 SHA Writer v01 上的本样本信号；不得称为正式资格。**C** 默认 `PATCH_NOT_RUN`，须在 A/B/P3/匹配全部冻结后由产品负责人另行授权，且只可做一次定向 Writer 修补与一次 Fidelity 复检，永不改写 A1/A2/B/P3 历史。

本轮不启动 Skill、runtime、自动化、分级生产、候选池新知识供给、Meta Prompt、来源审核降级或全书扩产。P1/P2/P3、独立来源审核和旁路诊断均不得取消。

## 3. 确定性材料选择（仅在总控放行后）

冻结材料为 `materials/T2.4-IDEA-PILOT-01/source.epub`，SHA-256 `3baf9932c92412f0e7d0ccae993182f55f62e2fab5ec46a3882d675476288664`。选择前只允许读取 EPUB 的 `toc.ncx`、ZIP member 名称／大小／二进制 SHA-256；不得读取任何 eligible chapter 正文。

从第 1—21 章完整作者章节中排除第 `7, 13, 14, 15, 16, 19` 章；第 2 章必须保留。按 member path 字典序映射匿名 ID，eligible count 必须为 15。payload 必须为 `T2.26|<source_sha256>|<eligible_id_1>\\n...`（真实 LF、末尾无 LF），精确 250 bytes、SHA-256 `16b480aa130a24d8fa1e3e925fb0eaa32740c732271691afd3cf87bdb27e70c3`；以 hash 前 16 个 hex 转无符号整数再对 15 取模。

控制层须从 source／TOC／metadata 独立完整运行两次；eligible mapping、payload、index、selected ID、chapter、member、size 与 SHA 必须一致，先冻结到 `control/material-selection-manifest.md`，才可读取 selected member。不同、空集、hash 不符、所选章节在排除集或任何人工替换均立即停止。

scope 首次写入只能以 `zipfile` 等二进制安全方法在内存切片，先计算 expected bytes/SHA，确认目标不存在后以排他 `wb` 写 `inputs/chapter-scope.xhtml`，立即二进制回读。不得文本解码、换行归一、shell 重定向或 `apply_patch`；任一不一致记 `INVALID_RUN`，保留首次事实且不得覆盖。

## 4. 阶段与 read-set 合同

1. **发现**：一名全新 Expert 仅读长期 owner、本卡和本轮 `inputs/chapter-scope.xhtml`；自主输出零到多个候选至 `working/candidate-pool-v01.md`，不得排序、成稿、联网或读取历史。目标／硬停止 5/8 分钟。
2. **比较与 P1**：独立比较者仅加读 candidate pool，逐项检查学习价值、组织、迁移、补造、成本、支配／互补，输出 `review/comparison-v01.md`。控制层机械装配中文优先连续可读的 `review/p1-product-review-v01.md`；产品负责人只判 `WORTH_EXPANDING`／`REJECT`，最多前二通过对象。未获逐对象中性 gate 前，不得展开、审核、Writer、Fidelity 或 Rater。目标／硬停止 4/6 分钟。
3. **旁路诊断**：全新隔离会话仅读章节与 candidate pool，输出 `diagnostics/candidate-breadth-v01.md` 的 `NO_VIABLE_CANDIDATE`、`NARROW`、`MULTI_FAMILY_SINGLE_LEVEL` 或 `MULTI_LEVEL_OBSERVED`；不反馈生产。目标／硬停止 4/6 分钟。
4. **展开、P2、来源**：每个 P1 通过对象从原 Expert 建隔离同源分支，输出独立知识结构、迁移、案例作用、边界和新增台账；P2 只判 `WORTH_PUBLISHING`／`REJECT`。每个 P2 通过对象由独立来源审核并只读必要课与中性 gate；需要时仅允许最小来源修正，核心改变即停止。保留独立来源审核，禁止直接改稿。
5. **Writer 与 Fidelity**：Expert 选择唯一实质锚点，输出含 lesson SHA、语义锁、边界的 handoff。全新 Writer 仅读来源冻结课、handoff 与 Writer 合同；不得读完整章节、Rater／Fidelity／产品意见或历史。Writer 只可重排、删重、解释既有术语、降负担，不得新增事实、机制、因果、结论、边界或锚点。v01 立即冻结 SHA。Fidelity 仅比较 lesson、handoff、Reader、edit ledger 的承重语义、边界、事实强度、unknown 与锚点；失败时每对象仅一次限定 v02 忠实性修正及一次复检。
6. **影子 Rater**：仅在 P1 后 Writer v01 出现时启动，且与 Fidelity 同 SHA。Rater 只读 target-reader 合同、Reader rubric、该 Reader 和最小 local context；不得读本卡、完整 Writer 指南、Expert、来源、edit ledger、Fidelity、历史、产品意见或完整章节。只判六题，逐问题输出 `issue_id, artifact_sha, issue_type, location, severity, owner, action, first_detected_by, status, why, meaning_to_preserve`，终局限 `BLOCKED`、`PATCH_ONCE`、`ACCEPTED_WITH_NOTES`、`ACCEPTED`，封存至 `sealed/`。Fidelity v01 通过才可 `RATER_RESULT_EVALUABLE`，否则仅 `EXPLORATORY_ONLY`。
7. **P3、揭盲与 C**：只装配通过 Fidelity 的 Reader，锚点唯一、移除评论后逐字恢复 B1。P3 包中文优先连续可读，不暴露 ID、Rater、成本或审计。产品先独立完成同 SHA 单篇六题与 issue，再判整章重复、密度、节奏、模板感及 `ACCEPTED/REJECTED`；整章问题不计 Rater 漏报。P3 冻结前不得揭封。其后机械 issue 配对要求 artifact SHA 相同、位置重叠、type/owner 相同、severity 相差最多一级且根因相同，结果只限 `MATCH/PARTIAL_MATCH/RATER_ONLY/HUMAN_ONLY/UNDECIDABLE`，产品确认语义。

## 5. A1/A2/B/C 与零结果

A1 单对象只能为 `FIRST_DRAFT_ACCEPTED`、`FIRST_DRAFT_ACCEPTED_WITH_NOTES`、`FIRST_DRAFT_NEEDS_PATCH`、`FIRST_DRAFT_FIDELITY_FAILED` 或 `FIRST_DRAFT_BLOCKED`；聚合只能为 `A1_FULL_TRANSFER`、`A1_PARTIAL_TRANSFER`、`A1_TRANSFER_NOT_DEMONSTRATED`、`A1_TRANSFER_NOT_OBSERVED`。A2 只能为 `PROSPECTIVE_FLOW_ACCEPTED`、`PARTIAL_TRANSFER`、`TRANSFER_NOT_DEMONSTRATED`、`VALID_ZERO_OUTPUT` 或 `INVALID_RUN`。合法零结果不证明 Writer 或 Rater 迁移，须记录 A1 未观察、Writer 未观察、Rater procedure 未执行、signal inconclusive。

Rater procedure 只能为 `RATER_PROCEDURE_VALIDATED/INVALID/NOT_EXERCISED`；signal 只能为 `PROMISING/INCONCLUSIVE/POOR`，且 `EXPLORATORY_ONLY` 绝不进入统计。C 仅在同 v01 被 Rater `PATCH_ONCE`、人类独立确认对应发布问题、无上游／来源／Fidelity／整章问题、Writer v01 Fidelity 通过并由产品另行授权时开始。

## 6. 预算、闭集、验证与停止

A+B 基线语义／评价调用上限 22，连同最多两次完全同输入技术重试上限 24；小组长最多 8 turns／40 分钟；组员累计 120 分钟。每次调用在 usage ledger 恰一条，服务端未给 token 则 JSON `null`／汇总 `unknown`，不得估算。C 不计 A+B，获准后每对象最多 Writer+Fidelity 各一次。

本轮只可写 `taskcards/T2.26-v04.md` 与 `runs/T2.26-PROSPECTIVE-TRANSFER-AND-SHADOW-RATER-04/` 内的 `control/`、`inputs/`、`working/`、`review/`、`diagnostics/`、`handoff/`、`sealed/`、`acceptance/`、`receipts/` 本卡指定产物；每个现存 artifact 必须由 registry 注册，registry 仅可有自身最终 bytes 无法自 hash 的单一 self-exemption。不得写长期 owner、其它 run/taskcard、README、共享 validator，亦不得 commit/push/reset/checkout/delete。

材料选择前、P1 前和收口前都必须验证：branch/HEAD 与身份、双次选择、scope expected/actual、五合同及 manifest SHA、freeze、registry、read-set、同 SHA、盲封时序、锚点唯一/B1 可逆、issue matching、预算及终局-产物一致。任一 source/身份/选择/first-write/合同/manifest/隔离/同 SHA/盲封/预算违规、修改合同或任务卡、Rater 泄露、Reader Rater 越权、用 Writer 修补上游、自动跳过产品门、需要读取禁区或扩大闭集，立即停止；如需变化，终止本 run 后由产品负责人确认新版本。

## 7. 当前授权停止点

本次授权只推进到合法 P1 中文审阅包。P1 包、候选池、比较、旁路诊断、ledger 与 registry 通过确定性核验后，生产小组长必须暂停；不得自行创建 P1 gate、启动 Expert 展开、P2、来源、Writer、Fidelity、Rater 或 P3。

## 8. 验收层级

- `implemented`：本卡与当前阶段授权的闭集产物存在。
- `integrated`：至少一份合法 Reader 已按同 SHA 路径装入完整 B1。
- `verified`：确定性材料、合同、读取闭集、盲封、同 SHA、锚点、可逆与统计检查通过。
- `accepted`：仅产品负责人分别给出 P1/P2/单篇/P3/匹配/C 判词。
