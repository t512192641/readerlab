# T2.41 ReaderLab Book Expert Teaching Skill v0.1.2 migration-boundary hardening

> blocker_permission: `none`
> closeout-result: `PASS`

## 1. 任务编号与标题

`T2.41`：在 `24512d27987a03add95fb81663418eed700bd6a6` 基线上，对
`readerlab-book-expert-teaching` Skill 做最后一轮迁移前工程修正；继续使用
`feature/readerlab-book-expert-teaching-skill-v0.1`，发布 `0.1.2`。

本卡不运行迁移样本、语义 Agent、Writer、Discovery、ABC 或完整编排器。0.1.0、0.1.1、0.1.2
的实现、run、fingerprint、产品 hash 和结果不得混算。

## 2. 目标与固定边界

- 0.1.0 根实现保持逐字不可变且可由根 `scripts/run.py` 复验；current dispatcher 移到 Skill 根
  `run-current.py`，不能污染旧 fingerprint。
- 新增独立 `versions/0.1.2/`；`CURRENT_VERSION` 为 `0.1.2`，dispatcher 使用三段数字版本并要求
  目标目录与 `VERSION` 精确一致。
- source map 是来源归属／证据边界；`source-allowlist.json` 是机器网络权限边界；`init` 生成闭合的
  `control/source-boundary.json`。
- 新增 `readerlab-book-expert-teaching/source-access/v1`。Expert 与 reviewer 各写一份 agent-declared
  访问回执；seal／verify／stage freeze／manifest／ledger／archive 均检查并保留它们。
- 保持六个命令、状态机、Expert／reviewer 隔离、双门、中文表达规则、通用产品问题、自然 M1/M2/M3/Writer
  正文不误杀、`not_integrated` 和不调用语义模型。

不得修改 `PRODUCT-DECISIONS.md`、`contracts/BOOK-CONTENT-FLOW-v2.md`、T2.36—T2.40 冻结产物、历史产品
判词、Writer／Discovery／ABC／总流水线或 0.1.0 根实现文件。

## 3. 允许读取清单

- `AGENTS.md`、`docs/current-task.md`、`docs/dev-state.md`、`docs/agent-run-ledger.md`、
  `PRODUCT-DECISIONS.md`、`ENGINEERING-LESSONS.md`、`blueprints/PIPELINE-MAP.md`、
  `audit/ASSET-LIFECYCLE-REGISTER.md`、`tools/run.py`、`tests/entry.py`、
  `tests/test_repository_audit_guardrails.py`、T2.40 taskcard。
- 当前 0.1.0 根 Skill、0.1.1 独立实现、Skill Creator `SKILL.md`、T2.38 replay fixture 登记的文件和 hash。

不打开新外部来源；不读取或复制 T2.36—T2.40 语义正文，不创建新的语义 run。

## 4. 交付文件清单

- `taskcards/T2.41.md`
- `docs/current-task.md`
- `docs/dev-state.md`
- `docs/agent-run-ledger.md`
- `docs/stage-implementation-status.md`
- `docs/expert-teaching-skill-v0.1-review-map.md`
- `docs/expert-teaching-skill-v0.1-review-brief.md`
- `docs/expert-teaching-skill-v0.1.2-change-receipt.md`
- `blueprints/PIPELINE-MAP.md`
- `audit/ASSET-LIFECYCLE-REGISTER.md`
- `.agents/skills/readerlab-book-expert-teaching/run-current.py`
- `.agents/skills/readerlab-book-expert-teaching/CURRENT_VERSION`
- `.agents/skills/readerlab-book-expert-teaching/versions/0.1.2/`
- `.agents/skills/readerlab-book-expert-teaching/tests/test_skill.py`
- `.agents/skills/readerlab-book-expert-teaching/tests/fixtures/t2.38-replay.json`
- `.agents/skills/readerlab-book-expert-teaching/tests/fixtures/t2.38-source-allowlist.json`
- `.agents/skills/readerlab-book-expert-teaching/tests/fixtures/t2.38-expert-source-access.json`
- `.agents/skills/readerlab-book-expert-teaching/tests/fixtures/t2.38-reviewer-source-access.json`

## 5. 硬约束

- 0.1.0 baseline aggregate fingerprint 固定为
  `4730c7392c67bd927b9d3609854141f6b8fb6297631381834d1d43275690233f`；根 fingerprint 扫描不得包含
  `run-current.py`。错版本回验必须使用从发布基线 `66eeaa1d8a068df3e73848717e20b0c87b105918` 临时物化的
  0.1.0 入口创建的 run，不得用升级后工作树新建的 run 冒充历史 run。
- allowlist 只允许 `allowed`、`reference_only`、`blocked` 与显式 redirect；不允许自动域名、查询参数或重定向扩张。
- `opened_urls` 只能是 `allowed`；`cited_only_urls` 可为 `allowed`／`reference_only`；数组互斥，blocked／新 URL 失败。
- source map 与 allowlist 必须闭合：所有 source-map URL 已登记，所有 canonical URL 已出现在 source map，额外 redirect 可登记。
- Skill-local replay 不调用模型或网络；manifest／ledger 的模型、reasoning、network、retry、Prompt 字段为
  `controller_declared`，Skill 自身 semantic calls 为 `none`。

## 6. 验证与停止

运行 Skill-local tests、quick validation、repository active tests、T2.38 mechanical replay、`git diff --check`
和敏感信息检查。已知资产穷举基线影子文件若仍失败，原样记录，不纳入本次提交。

停止时：`implemented` 为 0.1.2 文件存在；`verified` 仅限确定性测试与回放；`integrated` 为
`not_integrated`；`accepted` 为 `unknown`。不得运行迁移样本或把本卡结果当产品接受。
