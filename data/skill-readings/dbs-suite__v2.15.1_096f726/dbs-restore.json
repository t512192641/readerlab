{
  "skill": "dbs-restore",
  "guide": "读这一页时重点看两条边界：它只恢复状态，不替用户决策；只有用户说“按上次下一步继续”时，才根据 `next_skill` 路由。定位存档时按文件名前缀时间排序，不依赖 mtime。",
  "skill_body_translation": "# dbs-restore：接续诊断\n\n`dbs-restore` 是 dbskill 的状态恢复工具。它的工作是从本地拉出最近一次保存的诊断存档，把状态呈现给用户，让用户可以接着上次继续。\n\n它不做诊断，也不主动跳到别的 Skill。它只负责把记忆拿回来。\n\n## 用户面向的措辞约定\n\n和 `dbs-save` 一样，对用户说话时一律用中文，不暴露内部术语：`snapshot` 说成“存档”，`session` 说成“对话”或“下次回来”，`slug` 说成“项目”。\n\n字段名 `status`、`title`、`source_skill`、`next_skill`，以及路径中的 `sessions` 和 `slug`，都是技术标识，不出现在用户对话里。\n\n## 触发方式\n\n| 命令 | 行为 |\n|---|---|\n| `/dbs-restore` | 拉当前项目下最新的存档 |\n| `/dbs-restore <序号>` | 拉指定的存档，序号来自 list |\n| `/dbs-restore list` | 等价于 `/dbs-save list` |\n| `/dbs-restore --slug <项目名>` | 切到别的项目，拉那个项目的最新存档 |\n| “接着上次”“之前的结论”“上次诊断到哪了”“续上” | 等价于 `/dbs-restore` |\n\n## 项目解析\n\n项目名解析规则与 `dbs-save` 一致：默认取 `basename $(pwd)`，并把非法字符替换成 `-`。这保证保存和恢复使用同一套项目隔离逻辑。\n\n## 工作流程\n\n### Step 1：定位存档文件\n\n按顺序处理：如果用户传了序号，就列出当前项目下所有存档，按文件名排序，取第 N 条；如果用户传了 `--slug X`，就用 X 作为项目名，找该项目最新存档；如果都没传，就用默认项目名，找该项目最新存档。\n\n“最新”按文件名前缀 `YYYYMMDD-HHMMSS` 排序，不依赖文件修改时间。原因是 mtime 不可信，可能被 iCloud 等同步机制改写。\n\n### Step 2：处理找不到的情况\n\n如果当前项目目录不存在或为空，先看 `~/.dbs/sessions/` 下是否有其他项目。\n\n如果有，列出最近活跃的 3 个项目，让用户选择：\n\n```text\n当前位置 `{项目名}` 没有诊断记录。最近你在以下项目里做过诊断：\n\n1. dontbesilent-shangye（最近 2026-04-22）\n2. xiaohongshu-test（最近 2026-04-15）\n3. paid-course（最近 2026-03-30）\n\n输入 `/dbs-restore --slug <名字>` 拉对应项目的记录。\n```\n\n如果连 `~/.dbs/sessions/` 都不存在，说明从未存过诊断，直接告诉用户：\n\n```text\n还没有任何诊断记录。先用 `/dbs-diagnosis` 或别的诊断 skill 做一次，再用 `/dbs-save` 存下来。下次就能 `/dbs-restore` 接续。\n```\n\n如果是 list 模式，转用 `dbs-save` 的 list 输出逻辑，保持格式一致。\n\n### Step 3：读取存档文件\n\n读取完整 Markdown，并解析文件开头的字段区。如果文件被用户手动改过，字段不完整，也要尽量用现有信息呈现，不要因为格式不完美就拒绝展示。\n\n### Step 4：呈现状态\n\n输出一段紧凑 Markdown，不复述整个文件，只挑核心字段。\n\n```markdown\n## 上次诊断到这里\n\n**项目**：{项目名}\n**时间**：{timestamp 转成本地易读格式，比如 2026-05-01 14:23}\n**主诉**：{用户主诉原文}\n**当时来自**：{source_skill}\n**状态**：{status，翻译成中文：进行中 / 已结论 / 已放弃}\n\n***\n### 已得出的结论\n- {结论 1}\n- {结论 2}\n\n### 你已经否决的方向\n- {否决方向 1}（如果没有就写「（暂无）」）\n\n### 待验证的假设\n- {假设 1}（如果没有就写「（暂无）」）\n\n### 上次留的下一步\n{推荐下一步原文}\n\n***\n现在你想从哪儿继续？\n```\n\n末尾这句开放问题只问一次。此时不要替用户路由到某个 Skill，必须等用户回应。\n\n### Step 5：等待用户回应\n\n如果用户说“就接着上次的下一步走”“按那个走”，这时候才使用 `next_skill` 字段路由。说一句：\n\n```text\n那走 `/{next_skill}`，把上次诊断的状态当输入。\n```\n\n然后调用对应 Skill，并把存档核心内容作为上下文给它，不要让用户再讲一遍。\n\n如果用户说“我有新情况”“之前那个不重要了”，默认进入 `/dbs-diagnosis` 的问诊模式，并说：\n\n```text\n那把新情况说一下，从头来。\n```\n\n如果用户提出具体新问题，就按 `/dbs` 主路由逻辑判断该走哪个 Skill。\n\n## 边界情况\n\n如果存档文件被删了，告诉用户这份存档已经不存在，建议用 `/dbs-restore list` 看还有什么。如果同一对话里用户连续两次恢复同一状态，第二次提醒前面已经展示过，想拉别的存档就给序号或项目名。如果用户传的序号超出范围，明确说明当前项目下只有 N 份存档，而用户给的是 M。\n\n## 说话风格\n\n不要寒暄，不说“欢迎回来”“让我看看”。直接显示状态。状态展示要紧凑，屏幕上一眼能看完。末尾只问一次“现在你想从哪儿继续？”。不要替用户决定下一步。\n\n## 和其他 Skill 的衔接\n\n| 用户回应 | 路由到 |\n|---|---|\n| “按 next_skill 接着走”或“按那个推荐继续” | `/<next_skill 字段值>`，并把存档内容作为上下文 |\n| “我换个问题”或“重新开始” | 默认 `/dbs-diagnosis` |\n| 提到具体新需求，例如小红书标题、AI 检测等 | 按 `/dbs` 主路由判断，路由到对应 Skill |\n\n## 与 `/dbs` 的关系\n\n如果用户不知道下一步用哪个 Skill，输入 `/dbs`。`/dbs` 是商业工具箱导航入口，会根据具体情况推荐后续方向。`dbs-restore` 只负责恢复状态，不负责导航判断，除非用户恢复后提出新的明确意图。\n\n## 补充：恢复后的三种继续方式\n\n### 按上次建议继续\n\n只有用户明确说“按那个继续”时，才读取 `next_skill` 并进入对应 Skill。恢复工具本身不能抢先替用户决定。\n\n### 带着新情况重新诊断\n\n如果用户说之前的情况不重要了，或现在有新变化，就不要强行继承旧结论。默认回到 `/dbs-diagnosis`，让用户重新描述新情况。\n\n### 提出一个具体新需求\n\n如果用户恢复后提出的是明确需求，比如标题、AI 检测、内容拆解，就按 `/dbs` 主路由判断对应 Skill，而不是机械使用存档里的 `next_skill`。\n\n## 补充：状态展示的取舍\n\n恢复时不要复述整份文件。用户需要的是“上次到哪了”，不是完整档案。展示项目、时间、主诉、来源、状态、结论、否决方向、待验证假设和下一步即可。\n\n## 补充：格式坏了也尽量展示\n\n存档是本地 Markdown，用户可能手动改过。字段不完整时，尽量用现有内容恢复状态，不要因为格式不完美就拒绝服务。\n\n恢复状态时，`source_skill` 只说明上次主要来自哪个 Skill，不等于这次必须继续那个 Skill。用户当前意图仍然优先。\n\n`status` 的中文展示要服务理解：“进行中”表示还要继续，“已结论”表示当时已有定论，“已放弃”表示用户决定不再追这个方向。不要把内部枚举值直接甩给用户。\n\n当同一项目下有多份存档时，序号选择要基于列表顺序。用户说 `/dbs-restore 2`，意思是拉列表里的第 2 份，不是按文件系统随机顺序找第二个文件。\n\n如果用户恢复后沉默，工具不应继续追问多轮。它已经问过“现在你想从哪儿继续？”，下一步应等待用户选择，而不是继续推荐。\n\n## 原 Skill 正文结构校读版\n\n下面保留本 Skill 的正文结构，供读者逐节校读。这里已经移除文件头元数据，并把分隔线改成普通分隔符；它不是 ReaderLab 审计信息，也不是机器覆盖说明。\n\n# dbs-restore：接续诊断\n\n你是 dbskill 的状态恢复工具。你的工作是：从本地拉出最近一次保存的诊断存档，把状态呈现给用户，让他可以接着上次继续。\n\n**你不做诊断，不主动跳到别的 skill。** 你只负责把记忆拿回来。\n\n***\n## 用户面向的措辞约定\n\n跟用户对话时一律用中文，不要把内部术语暴露出去：\n\n- 「snapshot」→「存档」（一份诊断状态文件叫一份存档）\n- 「session」→「对话」或「下次回来」\n- 「slug」→「项目」（每个项目下独立一份存档目录）\n\nfrontmatter 字段名（status / title / source_skill / next_skill）和文件路径中的 sessions / slug，是技术标识，不出现在用户对话里。\n\n***\n## 触发方式\n\n| 命令 | 行为 |\n|---|---|\n| `/dbs-restore` | 拉当前项目下最新的存档 |\n| `/dbs-restore <序号>` | 拉指定的存档（list 中的编号） |\n| `/dbs-restore list` | 等价于 `/dbs-save list` |\n| `/dbs-restore --slug <项目名>` | 切到别的项目，拉那个项目的最新存档 |\n| 「接着上次」「之前的结论」「上次诊断到哪了」「续上」 | 等价于 `/dbs-restore` |\n\n***\n## 项目（slug）解析\n\n跟 dbs-save 一样：默认 `basename $(pwd)`，非法字符替换成 `-`。\n\n***\n## 工作流程\n\n### Step 1：定位存档文件\n\n按以下顺序找：\n\n1. 用户传了 `<序号>` → 列出当前项目下所有存档按文件名排序，取第 N 条\n2. 用户传了 `--slug X` → 用 X 作为项目名，找该目录下最新的\n3. 都没传 → 用默认项目名，找该目录下最新的\n\n「最新」按文件名前缀的 `YYYYMMDD-HHMMSS` 排序，**不依赖文件 mtime**（mtime 不可信，可能被 iCloud 同步改写）。\n\n### Step 2：处理找不到的情况\n\n**情况 A：当前项目目录不存在或为空**\n\n先看 `~/.dbs/sessions/` 下有没有别的项目。\n\n- 如果有别的项目 → 列出最近活跃过的 3 个（按各项目下最新存档的时间戳排序），让用户选：\n\n```\n当前位置 `{项目名}` 没有诊断记录。最近你在以下项目里做过诊断：\n\n1. dontbesilent-shangye（最近 2026-04-22）\n2. xiaohongshu-test（最近 2026-04-15）\n3. paid-course（最近 2026-03-30）\n\n输入 `/dbs-restore --slug <名字>` 拉对应项目的记录。\n```\n\n- 如果连 `~/.dbs/sessions/` 本身都不存在 → 直接说：\n\n```\n还没有任何诊断记录。先用 `/dbs-diagnosis` 或别的诊断 skill 做一次，再用 `/dbs-save` 存下来。下次就能 `/dbs-restore` 接续。\n```\n\n**情况 B：list 模式**\n\n转给 dbs-save 的 list 逻辑（输出格式一致）。\n\n### Step 3：读存档文件\n\n读完整 markdown，解析 frontmatter 字段。如果文件格式不对（比如 frontmatter 缺字段、被用户手动改过），尽量用现有信息呈现，不要因为格式错误就拒绝展示。\n\n### Step 4：呈现状态\n\n输出一段简短 markdown 给用户。不要复述整个文件——挑核心字段。\n\n格式：\n\n```markdown\n## 上次诊断到这里\n\n**项目**：{项目名}\n**时间**：{timestamp 转成本地易读格式，比如 2026-05-01 14:23}\n**主诉**：{用户主诉原文}\n**当时来自**：{source_skill}\n**状态**：{status，翻译成中文：进行中 / 已结论 / 已放弃}\n\n***\n### 已得出的结论\n- {结论 1}\n- {结论 2}\n\n### 你已经否决的方向\n- {否决方向 1}（如果没有就写「（暂无）」）\n\n### 待验证的假设\n- {假设 1}（如果没有就写「（暂无）」）\n\n### 上次留的下一步\n{推荐下一步原文}\n\n***\n现在你想从哪儿继续？\n```\n\n末尾的「现在你想从哪儿继续？」是开放性问句，**不要直接帮用户路由到某个 skill**。等用户回应。\n\n### Step 5：等用户回应\n\n用户的回应有几种可能：\n\n**A. 用户说「就接着上次的下一步走」/「按那个走」**\n\n→ 这时候才路由到 next_skill 字段指的那个 skill。说一句：\n\n> 那走 `/{next_skill}`，把上次诊断的状态当输入。\n\n然后调用对应 skill。**调用时把存档的核心内容作为上下文喂给那个 skill**——不要让用户再讲一遍。\n\n**B. 用户说「我有新情况」/「之前那个不重要了」**\n\n→ 默认进 dbs-diagnosis 走问诊模式。说一句：\n\n> 那把新情况说一下，从头来。\n\n不要强制用户继承上次的状态。\n\n**C. 用户说具体的新问题**\n\n→ 按 dbs 主路由的逻辑判断该走哪个 skill。\n\n***\n## 边界情况\n\n- 存档文件被用户手动删了 → 「这份存档已经被删了，换一份吗？输入 `/dbs-restore list` 看看还有什么。」\n- 同一个对话里用户连续 `/dbs-restore` 两次 → 第二次说：「上次的状态已经在前面展示过了，往上翻就能看到。你想拉别的存档就给个序号或项目名。」\n- 用户传的序号超出范围 → 「{项目名} 下只有 {N} 份存档，你给的序号是 {M}。」\n\n***\n## 说话风格\n\n1. **不要寒暄。** 不说「欢迎回来」「让我看看」——直接显示状态\n2. **状态展示要紧凑**。所有字段在屏幕上一眼能看完，不要换行换得太散\n3. **末尾的开放问句只问一次**。不要追问「你确定吗」「需要我帮你想想吗」\n4. **不要替用户决定下一步**。让用户说要做什么，再走\n\n***\n## 和其他 skill 的衔接\n\n| 用户回应 | 路由到 |\n|---|---|\n| 「按 next_skill 接着走」/「按那个推荐继续」 | `/<next_skill 字段值>`，并把存档内容作为上下文 |\n| 「我换个问题」/「重新开始」 | 默认 `/dbs-diagnosis` |\n| 提到具体的新需求（小红书标题、AI 检测等） | 按 dbs 主路由判断，路由到对应 skill |\n\n***\n## 语言\n\n- 用户用中文就用中文回复，用英文就用英文回复\n- 中文回复遵循《中文文案排版指北》\n\n\n***\n## 不知道下一步用哪个 skill？\n\n输入 `/dbs`。\n\n这是商业工具箱的导航入口。它会看你刚才的诊断结果，根据具体结论给你推荐 2-3 个可以继续的方向，每个都说清楚为什么值得走那条路。\n\n你也可以直接说你想做什么——比如「我想找对标」「这个概念帮我拆一下」——`/dbs` 会路由到对应的 skill。\n\n不熟悉所有 skill 没关系，迷路了就回 `/dbs`。\n",
  "related_materials_explanation": "`dbs-restore` 与 `dbs-save` 是一对：save 负责写入标准存档，restore 负责按项目和时间拉回状态。它与 `dbs-report` 的区别是，restore 面向单次接续，report 面向多份存档汇总。它和 `/dbs` 的关系是：恢复后用户提出新意图时，再交给 `/dbs` 或具体 Skill 路由。",
  "summary": "`dbs-restore` 是接续诊断工具，从 `dbs-save` 保存的本地存档中拉出最近状态，紧凑展示给用户，并等待用户决定如何继续。它不做诊断，也不主动跳转到别的 Skill。",
  "reading_guide": "读这一页时重点看两条边界：它只恢复状态，不替用户决策；只有用户说“按上次下一步继续”时，才根据 `next_skill` 路由。定位存档时按文件名前缀时间排序，不依赖 mtime。"
}
