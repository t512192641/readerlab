# ReaderLab 新项目边界

## 先读什么

每个新任务按以下顺序读取，读到足以完成当前任务即可：

1. `AGENTS.md`
2. `PRODUCT-DECISIONS.md`
3. `ENGINEERING-LESSONS.md`
4. 涉及生产流程、角色责任、晋级、实验治理或任务卡设计时，必须读 `blueprints/PIPELINE-MAP.md`
5. `GOLD-STANDARDS.md` 只在当前任务逐文件授权时读取；生产任务永远跳过
6. 只有涉及借鉴方法时，才读 `references/BOOK-AND-SKILLS-METHODS.md`
7. 任务卡只在执行对应任务时读取；例如 `taskcards/T2.4.md` 不构成其他任务的默认上下文

## 唯一事实边界

- 产品目标、用户、两条产品线和长期责任只以 `PRODUCT-DECISIONS.md` 为准。
- 历史工程观察、失败教训和不能外推的边界只以 `ENGINEERING-LESSONS.md` 为准。
- 正例、反例和边缘案例只以 `GOLD-STANDARDS.md` 及其链接的 `examples/` 文件为准。
- `blueprints/PIPELINE-MAP.md` 是两条长期产品线的派生架构地图，不拥有新产品决议；任务卡和实验组织不得改写其中的长期流程。
- 一次运行的材料、调用、角色构成、Idea 上限、模型档位、返工和停止参数只由对应任务卡拥有；任务结束即按卡失效，不得外推为长期规则。
- `references/BOOK-AND-SKILLS-METHODS.md` 只保存借鉴结论，不证明已经采用、实现或验证。

一个事实只能在一个 owner 中拥有正文。派生文档若与 owner 冲突，必须停止并报告，不能自行选择更顺手的版本。

## 旧项目隔离

- 不搜索、读取、复制、软链、导入或依赖任何旧 ReaderLab 项目、旧 Prompt、旧代码、旧合同、旧 schema、旧 runtime、旧 run、旧 Memory、旧任务或旧报告。
- 不根据旧项目当前仍存在于本机，就把它当作证据或开发入口。
- 本项目缺少的信息一律标为 `unknown`。只有产品负责人明确提供新材料或批准一次定向取证，才能补充；不得自行回旧项目“捞资料”。
- 外部参考对象只按本项目的方法卡使用，不安装、不接入、不复制其代码或 Prompt，除非产品负责人未来单独批准。

## 产品与技术责任

- 产品负责人决定产品目标、体验判断、优先级和最终接受。
- Codex 负责技术方案、实现、风险、验证和简洁说明，不把技术设计转嫁给产品负责人。
- 默认使用中文，面向不懂技术的产品负责人解释。
- 产品判词只能由产品负责人给出；模型评分、机器通过、流程跑通和技术负责人意见都不能替代。

## 工作模式、授权与正式写入

### 四种工作模式

- 工作模式分为讨论、诊断、方案和实施。非简单任务开始时声明一次当前模式；只有模式变化时再次声明，不在每条回复中机械重复。
- 讨论、诊断和方案模式只允许澄清、只读取证、分析和形成候选方案；不得写入仓库、创建生产任务或派发实施。
- 实施模式只能在产品负责人批准明确方案后开始，并且只能执行获批范围内的修改、委派和验证。

### 授权持续性

- 实施授权绑定于获批方案、目标范围、允许修改的文件或路径、验证标准和停止点；这些边界不变时，授权持续有效。
- 在获批范围内，临时候选区的机械修正、确定性检查重跑、相同输入和语义下的重新生成、执行会话失败后的同范围重新委派，以及不改变语义、公共合同、产品门和历史 artifact 的技术恢复，不需要重新请求批准。
- 改变目标或范围、公共合同、语义内容、正式 artifact 类型、历史证据、长期工具或平台，或者改变获批停止点时，必须停止实施并退回诊断或方案模式。

### 总控、Skill 与委派

- 总控负责澄清目标、守住模式和授权、拆解阶段并委派、持续等待任务完成、读取直接受托者的完整结果、独立验收并主动回传；生产波次只直接管理负责的小组长，不逐件追踪组员；不得把已派发、文件存在或执行者自报完成当作验收。
- 总控不得亲自创作、摘要、重排或改写语义 artifact；可以触发已经验证的确定性工具，生成授权、停止、冻结、核验和 gate 类控制 artifact。
- 在宣布使用或派发 Skill 前，必须先读取并核对其适用条件、禁止条件、输入和写入范围；不得仅凭名称或简介承诺使用。

### 候选验证与 promotion

所有正式 artifact 依次经过：

1. 在正式路径之外的候选区生成；
2. 完成适用的字节、类型、字段、来源、synthetic 和版本检查；
3. 全部检查通过后，排他 promotion 到正式路径；
4. promotion 后冻结；
5. 冻结后才进入下一阶段。

正式路径不得用于试写、修正或预检。结构化版本字段只能按字段修改，禁止通过全局文本替换更新。

### 新错误处理

- 发现错误后，不得只修当前表现并立即启动下一版本；必须先检查同类操作是否还存在于其他阶段，包括 exact-byte 写入、长文本复制、版本字段修改、fixture/official 边界、机械装配和状态同步。
- 同类机械问题在既有授权范围内一并处理；需要改变范围、合同、语义或停止点时，退回诊断或方案模式。

## 开发顺序

- 当前项目只有产品与证据基线，没有已实现 runtime。
- 正例生产任务必须先核对 `PRODUCT-DECISIONS.md` 与 `blueprints/PIPELINE-MAP.md` 的长期边界，再读取本任务卡；任务卡只能组织一次实验，不能新增长期角色、改写长期流程或决定最终 Skill 拆分。
- 后续开发从 `blueprints/PIPELINE-MAP.md` 的节点逐项建立合同、输入、输出、硬阻塞和验证，不得把蓝图中“尚未决定”的内容擅自定案。
- 图书线与 Skills 线分别开发和验收；共享机制不能让两条线共用错误的产品标准。
- 每次只验证一个真实材料范围和一个关键问题。模型调用、真实材料运行、新依赖、外部写入和云端 push 都需要当前任务明确授权。

## 验收语言

交付时始终分开：

1. `implemented`：文件或代码存在；
2. `integrated`：真实入口已经使用；
3. `verified`：确定性或真实运行证据证明目标路径；
4. `accepted`：产品负责人对需要产品判断的结果明确接受。

任何一层都不能替代另一层。无法证明时写清 `unknown`，不得用“已整理”“已接入”或“已完成”跨级。

## 完成报告完整性

- 每次任务完成报告的第一项必须是“本轮创建或修改过的全部 run 和文件清单”。
- 清单必须包括已取消、半成品、未晋级、被转作他用或仅作证据的对象；每项一行，格式为“路径 + 一句话状态”。
- 漏报任何本轮创建或修改对象，均视为报告不完整。

## Git

- 只使用本地 Git 保存可回退版本；本项目不配置远端、不 push。
- 提交前先验证目标路径并检查 diff；只提交当前任务相关文件。
- 删除、覆盖、迁移外部资产、增加运行依赖或改变长期产品边界，仍需产品负责人明确授权。

## 生产、诊断与验收读取隔离

- **生产端**（材料进入、B1、发现、知识卡、审核、Writer、B2 与阶段 3 的生产任务）永远禁止读取 `GOLD-STANDARDS.md` 与 `examples/`，也不得读取本次运行之外的产物或验收答案。
- **透镜开发／诊断端**只能在其任务卡逐项列出 `GOLD-STANDARDS.md` 与所需样张路径时读取；其产物是诊断或合同证据，不自动构成产品 gate 或生产许可。
- **裁判／回归端**只能在冻结后的独立验收、资格考试或回归任务中，按任务卡逐项读取金标、样张和本次冻结产物；其结论不得替代产品负责人判词。
- 任何读取例外只由当前任务卡逐文件授予，不得跨任务、跨 run 或跨会话继承。未被逐项授权的信息一律为 `unknown`。
