# ReaderLab 第一次项目系统大审计

> 审计日期：2026-07-25
> 审计类型：第一次项目清理大审计
> 业务证据基线 commit：`8c45281b0bec4dc82f15b8deb30c5f07ce9e6efb`；本轮纯审计提交不改变业务现场
> 总体结论：`RED`
> 模式：生产仓库只读；仅修改 `audit/` 自有文件

## 1. 本轮创建或修改过的全部 run 和文件清单

### 项目内正式文件

- `audit/ENGINEERING-AUDITOR-CHARTER.md`：升级到 v1.2，加入最小审计控制面、临时审阅责任和角色删除测试。
- `audit/AUDIT-OPERATING-MODEL.md`：新增审计类型、触发条件、冷启动入口、证据包、结果和关闭规则。
- `audit/CAPABILITY-MAP.md`：新增两条产品线与共享工程能力的成熟度投影和证据坐标。
- `audit/FINDINGS-REGISTER.md`：新增唯一行动门、开放问题、处理责任和复核条件。
- `audit/ENGINEERING-AUDIT-2026-07-25.md`：仅增加历史／superseded 标记，不改写原审计事实和原判断。
- `audit/PROJECT-SYSTEM-AUDIT-2026-07-25.md`：记录本次四轴只读大审计、结论和决策包。

### 项目内 run

- 无创建、修改、移动或删除。

### 项目外工作文件

- 无，0 个。

## 2. 固定点、范围与未读内容

### 2.1 业务现场快照

- 业务证据基线 commit：`8c45281b0bec4dc82f15b8deb30c5f07ce9e6efb`
- `taskcards/T2.35.md`：未提交业务修改，SHA-256 `740717e803b38490a360e00e0badf4e89085b740447254f860fd9edf8560dbc6`
- T2.35 run：`runs/T2.35-CH08-D3-EXPERT-P2-01/`，未跟踪
- `run.json`：SHA-256 `a625f7918ca7d05b64f29822d9472512c718156d38f06ad54ca2c98a465d53af`
- `production-freeze.json`：SHA-256 `0bde93d07d98e9c7d66fb29a2e2c6c463314b2aa4f723a945e5f4c65af40d385`
- `acceptance/acceptance-freeze.json`：SHA-256 `c981641cda9133258ce51d5a1c9808c934a309ccc792055f064d7b54f0e387eb`
- `acceptance/acceptance-report.md`：SHA-256 `4c0248572786e72cbfa0dffed63c15f745a0a47243b5bc5c30221a277f6d101f`
- 产品判词写回坐标：`runs/T2.35-CH08-D3-EXPERT-P2-01/acceptance/product-verdicts.md`，当前不存在

审计文件的本轮修改不作为业务成熟度证据。

### 2.2 审计范围

四条独立审阅线：

1. 冷启动 interface；
2. 流程、角色和关卡；
3. 状态、目录和工程健康；
4. 简洁性、成本和过拟合。

### 2.3 明确未读

- 未读取 `GOLD-STANDARDS.md`；
- 未读取 `examples/` 正文；
- 未读取旧 ReaderLab；
- 未以其他 run 的语义正文重新评价产品内容；
- 未替产品负责人判断 T2.35 Expert 是否值得进入 Writer。

## 3. 结论

总体评级为 `RED`。

当前允许的唯一产品前进行动，是产品负责人阅读已经冻结的 T2.35 审阅包，判断 D3 Expert 是否值得进入 Writer。只读审计和不写生产文件的方案设计是辅助权限，不是新的产品推进。

当前不得启动下一次正式生产 run。阻塞不是某一篇内容一定失败，而是：

1. 无法从一个可信状态 owner 恢复真实现场；
2. 发现、Expert、Writer 和 B2 的旧长期合同与现行产品 owner 冲突；
3. 默认测试、验证器和 run check 不能提供可信健康信号；
4. 研发门、长期责任和最终 Judge 尚未分层；
5. 近期规则持续增长，但新组合没有 P3 接受或未见材料复现；
6. 历史、当前、草案和本地载荷没有统一生命周期。

## 4. 证据真正证明到哪一层

| 对象 | 当前最高状态 |
|---|---|
| 审计控制面文件 | `implemented`；尚未接入项目唯一状态入口 |
| 冷启动 interface | 第一版、第二版独立测试均 `FAIL`，问题已用于收紧当前候选 |
| T2.35 | 工件存在并冻结；除根目录 `.DS_Store` 外，freeze 内容可复算 |
| T2.35 官方 run check | 当前 `FAIL`，不能继续声称现场完整 technical PASS |
| T2.35 产品状态 | `unknown`，尚无产品判词文件 |
| 图书完整流程 | 多项局部实验，不具备端到端资格 |
| Skills 包解读流程 | 没有当前运行证据 |
| 最终 Judge | 合同存在，历史资格 `FAIL`，不得上岗 |

## 5. 完整功能责任与角色删除测试

### 5.1 图书线不能缺少的功能责任

```text
材料识别、来源和范围冻结
  → B1 完整作者单元
  → 章节论证地图（只供控制层）
  → 认知背景／正式透镜与临时 Expert 路由
  → Expert 完整自由阅读、自然发现与允许沉默
  → 课程入口聚类、重复／偏科／停止判断
  → 低成本内容价值晋级
  → 同一 Expert 完整展开
  → 来源、事实、归属和边界核验
  → 承重语义锁
  → Writer
  → Fidelity
  → B2 锚定与确定性装配
  → 阅读完成声明
  → B3
  → 产物冻结
  → 最终独立语义验收
  → 产品负责人接受与治理反馈
```

这是一张功能责任图，不代表必须存在相同数量的 Agent、Skill 或模型调用。

### 5.2 主要角色和关卡结论

| 责任 | 审计结论 | 长期处理 |
|---|---|---|
| 发现 | 必须保留；旧合同与自主发现路线冲突 | 重开 interface，允许沉默，不由控制层派题 |
| 视角路由 | 功能必须保留 | 优先作为控制／编排责任，不必独立 Agent |
| 四向比较 | T2.34 的研发门 | 可并入课程入口版图选择，不能证明生产自主发现 |
| P1 | 名称和具体 C4 只属研发期 | 保留早期价值否决功能，不冻结 `P1` 为长期 interface |
| P2 | 当前语义漂移，已混合多种判断 | 拆清内容价值、来源事实和 Writer 准入；`P2` 仅作任务标签 |
| 来源审核 | 判断对象独立 | 当前不与内容价值或 Fidelity 合并 |
| Expert | 核心功能必须保留 | 是否独立 Agent 是实现选择 |
| Writer | 编辑责任必须保留 | T1.6 单卡一稿合同必须重开 |
| Fidelity | 当前有独立否决价值 | 资格前不并入 Writer 自检；未来可用证据评估降本 |
| B2 装配 | 必须保留为确定性功能 | 不需要成为语义 Agent；旧格式合同需重开 |
| B3 | 长期责任必须保留 | 当前无合同、实现或样张 |
| 最终 Judge | 冻结后独立语义验收必须保留 | 具体模型和拆分未知；当前资格失败 |
| P3 | 研发和资格阶段的产品接受 | 不默认成为最终 Skill 每章必经的后台角色 |

删除测试表明：来源审核与 Fidelity 分别防止“上游知识不可靠”和“下游编辑改坏语义”，目前不能因名称相似而合并。最可能需要合并的是“方向比较与课程版图选择”，最需要拆开的是当前混义的 P2。

## 6. 长期合同冲突

### 6.1 发现与 Expert

T1.3／T1.4 仍以三字段透视需求、路由和一句候选主张作为上游接口；现行产品 owner 要求 Expert 完整自由阅读、自然决定教什么，控制层不替 Expert 命题。旧接口不能直接成为下一次完整生产链。

### 6.2 Writer

T1.6 要求一张锁定知识卡对应一个 Writer 成品，Writer 不得拆卡；现行产品 owner 允许完整专家初稿包含多张相连知识卡，也允许 Writer 必要时拆分。两者直接冲突。

### 6.3 B2

T1.7 固定旧 callout 形式，而近期任务使用 Reader 命名，真实 `tandem-comments` 接口仍未冻结。机械装配责任有效，旧展示格式不能直接上岗。

### 6.4 Judge

T1.8 只冻结长期五项语义和隔离边界，不证明 Judge runtime、模型或资格。T2.2 的历史结果仍是 `FAIL`。

## 7. 状态、目录与工程健康

### 7.1 状态 owner 冲突

- README 自称唯一入口，仍指向 T2.12；
- T2.12 实际已经完成；
- execution roadmap 仍把 T2.12 写成下一轮；
- pipeline map 当前停点停在 T2.10；
- docs 状态说明只到 T2.25；
- 当前业务现场实际是未提交的 T2.35 `P2_PASS_READY_FOR_PRODUCT_REVIEW`。

### 7.2 默认健康信号失真

- `python3 -B -m unittest discover -v`：0 项测试，退出 5；
- `python3 -B -m unittest discover -s tests -v`：35 项中 24 PASS、1 FAIL、10 ERROR；
- 失败主要绑定历史 T2.26 的旧 Writer bytes、旧 HEAD 和旧任务卡 hash；
- `python3 -B validate.py`：拒绝 T2.12—T2.35 等现有任务卡，并报告旧 manifest／路径／字段问题；
- 仓库没有定义 active 默认测试的配置入口。

### 7.3 run 生命周期

- 65 张任务卡，约 11,771 行；
- 42 个 run，456 个文件；
- 183 个 run 文件被 Git 跟踪；
- 268 个 run 文件被 `.gitignore` 忽略；
- 仅 1 个 archive；
- 当前 `run.py` profile 下，7 个 run 可通过，31 个是无 `run.json` 的旧 profile，4 个只因 `.DS_Store` 失败。

这不说明 31 个旧 run 已损坏，只说明项目没有 profile、lifecycle、retention 和 replay 的统一投影。

### 7.4 `.DS_Store` 事故

受影响 run：

- `runs/T2.28-WRITER-COMPRESSION-02/`
- `runs/T2.30-C008-END-TO-END-01/`
- `runs/T2.31-C008-WRITER-PATCH-01/`
- `runs/T2.35-CH08-D3-EXPERT-P2-01/`

跳过该宿主元数据后，四个 run 的冻结清单仍一致。根因是 artifact inventory 与 `.gitignore` 规则不一致，不是语义工件已被修改。

## 8. 简洁性、成本与过拟合

### 8.1 近期产品结果

T2.28、T2.30、T2.31、T2.33 四次近期 P3 均未接受。T2.35 只到 P2，尚无 Reader 和 P3。

### 8.2 可量化浪费

- T2.33 总墙钟 69 分钟，其中 Chrome remote debugging 46 分钟，占约三分之二；
- T2.34 用 6 个语义角色、约 24 分钟，只完成方向比较；
- T2.35 约 19 分钟，其中 5 分钟是等待／工具阻塞，仍只到 Expert 与来源／P2；
- T2.28—T2.35 八张任务卡共 1,810 行；
- T2.30 曾产生 10 个临时脚本，后续才盘点收敛。

Chrome 路线已被后续任务禁用，这是有效纠偏；但“文档禁止”仍缺少统一工具层回归证明。

### 8.3 规则累积

近期依次增加或调整：

- 外部知识承重 C4；
- Writer 前置指令和 patch；
- 单句自检；
- 范围和可掌握性检查；
- 四方向比较；
- 单方向 Expert／P2。

每项都有局部失败依据，但组合后尚无新 P3 接受。下一步再针对第八章加长期规则，会扩大过拟合风险。

## 9. 保留、逻辑归档与物理清理候选

### 必须保留

- 产品 owner、工程经验、长期边界；
- T2.35 任务卡、冻结 run、P2 证据与产品待审状态；
- 所有产品判词、freeze、receipt、hash、失败 blocker 和 invalid-run 证据；
- `tools/run.py` 的排他 promotion、身份和 freeze 能力；
- 能证明工程事故、失败边界和修复边界的最小历史控制证据。

### 逻辑归档候选

先从默认入口隐藏，不移动文件：

- 已完成或失效的历史任务卡；
- 31 个旧-profile run；
- T2.26 多版本与 recovery；
- 过期状态文档；
- README 和 execution roadmap 中的大段历史恢复正文；
- 已与现行 owner 冲突的旧合同和历史专用验证。

### 物理清理候选

仅分类，不授权操作：

- 四个 `.DS_Store`；
- 已被 tracked control evidence 完整覆盖且不再承担唯一证据的 ignored payload；
- 经引用分析确认没有唯一证据价值的临时截图、候选和重复本地载荷。

任何移动或删除前，必须先有引用清单、恢复证明和产品负责人明确授权。

## 10. 对最终 Skills 交付中两条产品线的影响

### 图书线

当前不是“再做完 Writer 和 Judge 就接近完成”。首先需要恢复无冲突的完整责任图、可信工程基线和跨样本复现条件。B3、最终 Judge、目标档资格和真实 Skill 编排仍是独立的大阶段。

### Skills 包解读线

E1—E4 全部没有当前运行证据。只能共享材料身份、范围、冻结、receipt、成本和故障恢复等薄工程骨架，不能复用图书 Writer、五项 Judge 或 P1/P2/P3 证明本线进展。

## 11. 推荐执行顺序

### 批次 A：下一次 run 前的工程恢复

1. 建立单一当前状态 owner；
2. 建立真正的 active 默认健康入口；
3. 将 T2.26 等历史检查迁到显式 historical replay；
4. 修复共享 artifact inventory，使 `.DS_Store` warning、其他未知文件 hard fail；
5. 让 T2.35 官方 `run.py check` 恢复通过。

### 批次 B：完整责任和合同校准

1. 逐项处理 T1.3、T1.4、T1.6、T1.7 的冲突；
2. 用无歧义名称区分课程入口选择、内容价值、来源事实、忠实性和产品接受；
3. 明确 P1/P2/P3 的研发期生命周期；
4. 形成完整功能责任图，但不预先决定 Agent 数。

### 批次 C：生命周期和认知成本

1. 为 42 个 run 和历史任务卡建立 profile、lifecycle、retention 和 replay 投影；
2. 逻辑归档历史对象；
3. 明确 tracked control evidence、ignored payload 与备份责任；
4. 完成引用审计后再提交物理清理方案。

### 批次 D：恢复产品实验

只有 A、B 中与下一实验直接相关的问题独立复核通过后，才能基于 T2.35 产品判词设计下一实验。若 T2.35 不接受，回到视角／Expert；若接受，才设计 Writer／Fidelity／B2，并冻结变量，不能继续在同章无限加规则。

## 12. 审计控制面验证

第一次全新上下文测试为 `FAIL`，发现：

- 冷启动读取闭集违反项目强制规则；
- 证据缺少精确坐标；
- Git 与工作树固定点混写；
- 多问题权限竞争；
- 冷启动信息过载。

修正后第二次测试仍为 `FAIL`，但已能准确恢复两条线、`RED` 总门、主要成熟度和禁止动作。剩余问题是：

- 没有显式区分唯一产品前进行动与辅助只读权限；
- T2.35 dirty 工作树缺少精确哈希和审阅包坐标；
- 没有按序的下一证据展开 manifest；
- 证据事实、派生投影、先前观察和当前复验未明确区分。

第三次测试只剩一个阻断缺口：部分证据坐标没有逐项标明 `owner fact`、`derived projection`、`prior audit observation` 或 `current-point reverified`。

补齐全部 E01—E13 的证据模式后，第四个全新上下文仅读取规定六个冷启动入口，结论为 `PASS`。它能够无猜测恢复：

- 两条独立产品线；
- `RED` 总门；
- T2.35 审阅这一项唯一产品前进行动；
- 辅助只读权限和禁止动作；
- T2.35 精确对象、hash 与 freeze；
- 成熟度投影边界；
- 下一证据展开 manifest；
- 阻止新 run 的状态、合同、健康和规则累积问题。

因此审计冷启动 interface 达到 `verified`。这只证明审计入口可用，不证明底层生产问题已经修复，也不解除 `RED`。

## 13. 当前决策包

### 不需要产品负责人决定

以下属于技术治理，可由执行现场提出方案：

- 状态 owner 的实现形式；
- active／historical 测试 adapter；
- artifact inventory 修复；
- run lifecycle 投影；
- 合同冲突分析和无歧义责任命名。

### 需要产品负责人后续决定

1. T2.35 D3 Expert 是否值得进入 Writer；
2. 第一次清理报告获得接受后，是否授权执行现场按批次 A、B、C 实施；
3. 物理删除候选出现后，是否授权具体删除范围；
4. 当两条线需要调整优先级或最终 Skill 包装时，再作产品选择。

### 当前停止点

本审计不授权生产修复、合同修改、目录清理、新 run、Judge 恢复或 Skill 化。下一步应先完成审计控制面独立验证，再把本报告和治理批次交产品负责人确认。
