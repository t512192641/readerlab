# T2.35 第 8 章 D3 神圣价值 Expert 展开与 P2

current-state: P2_PASS_READY_FOR_PRODUCT_REVIEW
experiment-id: T2.35-CH08-D3-EXPERT-P2-01
planned-run-path: runs/T2.35-CH08-D3-EXPERT-P2-01
current-stop-point: P2_PASS_READY_FOR_PRODUCT_REVIEW
production-started: yes
selected-direction: D3_SACRED_VALUES_ADVANCE_TO_EXPERT

## 1. 唯一目标

产品负责人已在 T2.34 将 D3 判为 `ADVANCE_TO_EXPERT`。本轮只把该方向从比较 brief
推进为完整 Expert 课，并由独立来源审核／P2 判断它是否真正具备进入 Writer 的资格：

`D3 Expert 完整课与知识卡 → 独立来源审核／P2 → 停止`

本轮不进入 Writer、Fidelity、装配或 P3。`P2 PASS` 只表示内容可以在后续获批任务中进入
Writer，不表示 Reader 已生成或产品已接受。

## 2. 材料、方向与三字段需求

- source：`materials/T2.4-IDEA-PILOT-01/source.epub`
- source bytes：`672638`
- source SHA-256：`3baf9932c92412f0e7d0ccae993182f55f62e2fab5ec46a3882d675476288664`
- 章节：第 8 章《宗教：神祇只是为国家服务》
- EPUB member：`text/part0013.html`
- member bytes：`34513`
- member SHA-256：`e46e57832afe5e89130a8884613ed62c0febc14e87c6cc59485328be7ba95bc8`
- scope：member 首字节至唯一 `</html>` 末字节，排除其后的唯一 `0a`
- scope bytes：`34512`
- scope SHA-256：`0f4af98ac9beb82cea8d4ad708191bf28aa7758645249e0640d6c1d9d5c4f88a`

本 run 从 source 重新机械提取自己的 `locked/chapter-scope.xhtml`，不得复制或读取其他
run 的 scope、brief、Expert、Reader、来源审核、比较或产品判词。

本轮三字段透视需求冻结为：

1. **位置锚点**：Expert 必须在完整章节中自行选出并逐字记录实际触发本课的段落；不得由
   控制层预截一句话代替完整阅读。
2. **为什么值得停下**：原文讨论信仰、民族与政治忠诚时，利益补偿和常规妥协为何可能
   失效；读者需要分清真正的神圣价值结构与“人有时不愿妥协”这一常识。
3. **期望增量类型**：`预测`，以可追溯的机制、权衡与适用边界承重。

给 Expert 的方向指令原文冻结为：

> 检验领土、信仰、民族尊严等对象被具体持有者视为不可交易后，为什么补偿、谈判和利益交换可能失效。必须区分真实的神圣价值知识结构与“人有时不愿妥协”这一原文可直推常识。完整展开只能交付一套窄而可检验的谈判／决策透镜，不能把它扩张为对国家神道、神风、宗教民族主义或现实国际冲突的全面历史诊断，也不能把象征性承认写成解决主权、安全或权力不对称的万能处方。

## 3. producer 与角色

本项目没有获授权的正式透镜资产或登记册可供本轮调用；本方向作为当前材料的真实知识
缺口，由一次运行专用的临时专家承接：

- `producer kind`: `temporary-expert`
- `producer id`: `t235-sacred-values-negotiation`
- `producer version`: `v01`

该身份只服务本 run，不自动晋升、复用或建立透镜登记册。

产品负责人批准本卡后，授权两次语义执行：

1. Expert v01：`gpt-5.6-sol / high`
2. 独立来源审核／P2 v01：`gpt-5.6-terra / high`

本轮没有返工或重试额度。审核发现可修正问题时也先停止，不能原位改写 canonical raw
输入。

## 4. Expert 完整展开

Expert 使用全新上下文，本地 read set 只有：

- 本 run 的 `locked/chapter-scope.xhtml`
- 第 2 节冻结的方向指令（直接置于任务 Prompt，不作为额外文件读取）

Expert 默认允许 `Web Search + Web Fetch`，最多 10 条独立查询、实际打开 20 个唯一来源
页面；上限不是配额，知识结构和关键来源足够后即停止。禁止 Chrome remote debugging、
浏览器自动化和 `web-access` Skill。必须记录查询、URL、用途与总数。

Expert 必须先完整阅读章节，再完成：

1. 明确这套知识原生回答什么问题，给出一句可反驳的承重主张；
2. 展开不可交易性、禁忌交换、物质加码可能反噬等必要关系，但不预设所有关系都成立；
3. 区分经验研究支持的关系、成熟理论、Expert 教学综合和应用到原文后的推论；
4. 说明哪一项新预测无法从原文直接推出，以及删除外部知识后读者会失去什么判断能力；
5. 以一个具有清楚方案对照和最低背景成本的案例走通主干；
6. 说明原文章节哪些材料被重新照亮、哪些没有被解释；
7. 给出一个低背景成本迁移与最重要失效边界；
8. 执行 T1.4 的四类伪增量探针、删除测试和去重预筛。

同一次 Expert 执行在 run 外生成两个候选：

- `raw/D3-expert.md`：完整 Expert 课；
- `raw/knowledge-cards.md`：包含与完整课一致的 1—N 张相互连接的知识卡。

知识卡数量由完整讲清本方向所需的承重节点决定，不设配额，也不得把旁支拆卡凑数。每张
卡必须机械可见 T1.4 的七项责任并保持本卡冻结的 producer lineage。完整课与知识卡
主张不一致、缺少七项责任、锚点无法回到章节或外部知识仍只是理论标签时，均不得
promotion。

## 5. 独立来源审核与 P2

来源审核使用全新上下文，本地 read set 只有：

- `locked/chapter-scope.xhtml`
- 当前正式 Expert 课
- `raw/knowledge-cards.md`

来源审核使用 `gpt-5.6-terra / high`，只用 `Web Search + Web Fetch`，最多 12 条独立
查询、实际打开 24 个唯一来源页面。禁止 Chrome remote debugging、浏览器自动化和
`web-access` Skill；优先经典原著、原始论文、正式出版物、权威出版元数据和高质量综述。

审核必须：

1. 逐项核验受保护／神圣价值、禁忌交换及物质激励反噬的实际来源、研究对象、对照条件
   和结论范围；
2. 特别核验“增加补偿可能被理解为收买并反噬”是否有直接证据，以及适用的是哪些具体
   持有者、议题和提案；
3. 将象征性承认的证据强度与物质反噬分开，不得把有限或短期结果升级为普遍解法；
4. 检查 Expert 是否把章节材料误写成研究已经证明的历史因果；
5. 对每张卡执行 T1.5 的价值、事实、重复、边界四维审核；
6. 重做 C4 删除测试：若去掉外部知识后核心仍可由原文推出，必须在 P2 停止。

每张卡都必须逐项保留 T1.5 的价值、事实、重复、边界四维结论与 lineage，并且只取得
以下四态之一：

- `锁定`
- `淘汰`
- `退回`
- `待补证据`

完整审核结果写入 canonical `raw/review-outcomes.md`。四态之外，审核还必须给出本 run
唯一派生 P2 状态，并按以下顺序匹配，命中即停止，不得同时给出两个状态：

1. `P2_STOP_C4_NOT_LOAD_BEARING`：核心主张的外部知识不承重；
2. `P2_STOP_CONTENT_NOT_QUALIFIED`：C4 未失败，但存在淘汰或退回项，或剩余锁定卡不足以支撑完整课程；
3. `SOURCE_BLOCKED`：前两项均未命中，但存在待补证据项，当前证据不足以完成 P2；
4. `P2_PASS_WORTH_WRITING`：全部承重卡均锁定、课程主干保持完整，且 C4 删除测试通过。

审核不得修改知识卡。锁定卡按 T1.5 原样写入 `locked/knowledge-cards.md`，未锁定卡只留在
raw；只有 `P2_PASS_WORTH_WRITING` 才生成 `locked/D3-p2-gate.md`。其他状态均停止，
具体修正或补证要求留在 `raw/review-outcomes.md`，不在本 run 返工。

## 6. 正式 artifact、审阅包与 promotion

所有正式 artifact 先在 `/private/tmp/readerlab-t235-*` 形成候选，完成适用的 UTF-8、
类型、字段、来源、synthetic、版本、lineage、read set、联网记录和父 artifact 绑定检查，
再通过 `tools/run.py promote` 排他写入。正式路径不得试写、修改或覆盖。

计划 artifact：

- `locked/chapter-scope.xhtml`
- `locked/chapter-readable.md`
- `raw/D3-expert.md`
- `raw/knowledge-cards.md`
- `raw/review-outcomes.md`
- 有锁定卡时 `locked/knowledge-cards.md`
- 通过时 `locked/D3-p2-gate.md`
- `acceptance/acceptance-report.md`

`locked/chapter-readable.md` 必须由本 run 的 XHTML 确定性转换并保持全文、顺序和文字内容，
只去除呈现标记；它不作为任何生产角色输入。`acceptance/acceptance-report.md` 必须把
可读完整原文、当前终局 Expert 课和来源／P2 结论放在同一份 Markdown 审阅包中，语义
正文 exact-byte 呈现，不摘要、不重排，便于产品负责人直接审稿。

## 7. 停止点

本轮只允许以下终局：

1. `P2_PASS_READY_FOR_PRODUCT_REVIEW`：外部知识与内容资格通过，审阅包冻结，等待产品负责人决定是否另开 Writer 任务；
2. `P2_STOP_C4_NOT_LOAD_BEARING`
3. `P2_STOP_CONTENT_NOT_QUALIFIED`
4. `SOURCE_BLOCKED`
5. `STOPPED_CONTROL_FAILURE`：越界读取、禁用工具、模型／reasoning 不符、结构或 lineage
   不完整、promotion／freeze 边界失效。

任何终局都不进入 Writer、Fidelity、装配或 P3。不得读取 T2.34 的 D3 brief、来源审核、
比较报告或产品判词来补强 Expert；D3 的唯一方向信息来自本任务卡。

## 8. 验证与墙钟记录

完成前至少验证：

- source、member、scope bytes 与 SHA-256；
- 实际模型、reasoning、调用次数及每次开始／结束时间；
- 每个角色的 read set、查询、URL、允许工具和预算；
- Expert 完整课与知识卡主张、锚点、七项责任和 lineage 一致；
- 来源审核四维结论、C4、来源身份、研究对象、对照条件与边界完整；
- 通过时 locked 卡与对应 raw 卡 exact-byte；
- 可读原文与 XHTML 的全文、顺序和文字内容等价；
- 审阅包中的原文、Expert 与 P2 正文分别 exact-byte；
- production／acceptance freeze 与 `tools/run.py check`；
- 仓库相关确定性检查、固定点 Standards／Spec 双轴审查与 Git diff。

完成报告增加分钟级墙钟表：

| 环节 | 开始 | 结束 | 墙钟分钟 | 终局／阻塞 |
|---|---|---|---:|---|
| 章节提取与可读转换 |  |  |  |  |
| Expert |  |  |  |  |
| 来源审核／P2 |  |  |  |  |
| 审阅包、freeze 与验证 |  |  |  |  |
| 等待／工具阻塞 |  |  |  |  |

本轮不计划创建临时脚本。若预计超过两个未纳入仓库的临时脚本，必须提前说明数量、理由
及无法复用仓库工具的原因。

## 9. 明确不做

- 不进入 Writer、Fidelity、装配或 P3；
- 不重跑开放发现或四向比较，不增加第五方向；
- 不读取或复用 T2.33、T2.34 或其他 run 的语义产物与验收答案；
- 不新增门、Rater、合同、候选类型标签、长期 schema 或控制框架；
- 不做裁判、透镜登记册或 T2.2 恢复；
- 不使用 Chrome remote debugging、浏览器自动化或 `web-access` Skill；
- 不修改 Writer v1.3、T1.4、T1.5 或其他长期合同；
- 不把本 run 临时专家自动晋升为正式透镜。

## 10. 写入授权

产品负责人过目本卡前，只允许创建或修改：

- `taskcards/T2.35.md`

产品负责人明确放行后，授权才扩大为：

- `taskcards/T2.35.md`
- `runs/T2.35-CH08-D3-EXPERT-P2-01/`
- 工作区外 `/private/tmp/readerlab-t235-*`

任务卡获批前不得创建 run、提取章节、派发语义角色或开始计时。本卡提交后停在
`TASKCARD_REVIEW`。

## 11. 启动记录

产品负责人已放行本卡。控制层于 `2026-07-25 04:06 -0400` 创建 run，重新从 source
机械提取第 8 章 scope，经候选绑定 validator promotion 为本 run 的
`locked/chapter-scope.xhtml`；当前停在 `EXPERT_PENDING`。

Expert 使用 `gpt-5.6-sol / high` 于 `2026-07-25 04:08–04:15 -0400` 完成一次执行：

- `raw/D3-expert.md`：`27640` bytes，SHA-256
  `22e8226fc131c74bb522e3f6a292b0e799dd3bac77116318455f24dd67f6b8c6`
- `raw/knowledge-cards.md`：`10123` bytes，SHA-256
  `cb7544ac6d5f80cf67adf1cb0dbab56ee7cb8df5b9efbc8cc31b5a351fac2655`

控制层只对 K02 “逐字”锚点的内层引号做了 exact-quote 机械校正；主张与语义未改。五张卡、
九项机械字段、五处章节锚点、producer lineage、10 条查询与 15 个唯一 URL 均通过
候选绑定 validator 并完成 promotion。当前停在 `P2_PENDING`，来源审核尚未启动。

## 12. P2 终局

独立来源审核使用 `gpt-5.6-terra / high` 于
`2026-07-25 04:19–04:21 -0400` 完成一次执行，使用 6 条查询并打开 8 个唯一 URL；
未使用 Chrome、浏览器自动化或 `web-access`。

- `raw/review-outcomes.md`：`17969` bytes，SHA-256
  `547f49a4ff341af45163f921d5806e2645b0aac1074cb3aec91c436aff837dff`
- K01—K05：五张卡的唯一四态终局均为 `锁定`
- run 级唯一派生状态：`P2_PASS_WORTH_WRITING`
- `locked/knowledge-cards.md` 与 raw 卡 exact-byte，SHA-256
  `cb7544ac6d5f80cf67adf1cb0dbab56ee7cb8df5b9efbc8cc31b5a351fac2655`
- `locked/D3-p2-gate.md`：`999` bytes，SHA-256
  `9ba2f3a6498890d1b88823810b5c39713084866c7d7daffa4679efe6d8b12e3d`

`acceptance/acceptance-report.md` 已把可读完整原文、终局 Expert 课和独立来源／P2 正文按
原字节顺序装入同一 Markdown 审阅包：`74805` bytes，SHA-256
`4c0248572786e72cbfa0dffed63c15f745a0a47243b5bc5c30221a277f6d101f`。
production 与 acceptance 均已冻结，`tools/run.py check` 通过。

本轮终局为 `P2_PASS_READY_FOR_PRODUCT_REVIEW`。这只证明 D3 具备后续 Writer 资格；
没有启动 Writer、Fidelity、装配或 P3，也不构成 Reader 或产品接受。

## 13. 产品判词

产品负责人于 2026-07-25 给出 `P2_PASS_WORTH_WRITING`。D3 不返回 Discovery 或 Expert，
下一步可在独立获批任务中进入 Writer；Reader 尚未生成，因此产品接受仍未发生。

完整产品判词、Writer 强制护栏和 Reader 最小主干见：
`runs/T2.35-CH08-D3-EXPERT-P2-01/acceptance/product-verdicts.md`。

### 环节墙钟

| 环节 | 开始 | 结束 | 墙钟分钟 | 终局／阻塞 |
|---|---|---|---:|---|
| 章节提取与可读转换 | 04:06 | 04:07 | 1 | 两项 promotion 通过 |
| Expert | 04:08 | 04:15 | 7 | 一次完成，5 张卡 |
| 来源审核／P2 | 04:19 | 04:21 | 2 | 5/5 锁定，P2 PASS |
| 审阅包、freeze 与验证 | 04:21 | 04:25 | 4 | 双 freeze 与 run check 通过 |
| 等待／工具阻塞 | 04:07–04:08；04:15–04:19 | — | 5 | Agent 调度等待；装配命令 1 次编码语法失败，未写正式路径 |

总墙钟约 19 分钟；并行或等待不与角色耗时重复相加。

### 仓库验证

- `tools/run.py check`：PASS
- acceptance 三段语义正文 exact-byte：PASS
- raw／locked 知识卡 exact-byte：PASS
- `test_run_promotion.py`：8/8 PASS
- `test_t226_material_scope.py`：7/7 PASS
- 全仓：35 项中 24 PASS、11 失败；失败仍全部属于历史专用 T2.26 的冻结 Git 身份、
  冻结父任务卡身份与旧 Writer 字节假设，不涉及 T2.35。本轮未修改 T2.26。
