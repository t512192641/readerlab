# ReaderLab 工程事故与防复发台账

> 快照日期：2026-07-25
> owner：工程事故的根因、防线、复验方式与剩余风险

## 结论

四类重复浪费中，`.DS_Store` 已有自动防线；Chrome 阻塞和临时脚本扩散已有模板级防线，
但还不是运行时强制；当前状态过期已经再次发生，仍是下一正式生产任务前必须修复的问题。

## 事故清单

| 事故 | 已确认影响 | 根因 | 当前防线 | 自动化程度 | 下一动作 |
|---|---|---|---|---|---|
| Chrome remote debugging 阻塞 | 产品负责人确认总浪费 49 分钟；T2.33 任务记录中可明确归因给 remote debugging 的分项为 46 分钟 | 来源审核选择了高故障、非必要浏览器路径 | `taskcards/TEMPLATE.md` 规定只用 Web Search／Fetch，禁止 Chrome 和浏览器自动化 | 文档约束；未运行时拦截 | 下一来源审核的变更检查核对工具记录；第二次出现立即 `RED` |
| 一次性临时脚本扩散 | T2.30 产生 10 个临时脚本，增加清理和复验成本 | 缺少稳定工具接口，机械动作按单次 run 硬编码 | `AGENTS.md` 要求预计超过 2 个先说明；T2.31 已登记并清理 | 流程约束；未自动盘点项目外文件 | 新任务完成报告核对项目外文件；相同机械动作第二次出现时先提取共享能力 |
| `.DS_Store` 污染 run check | 4 个 run 出现宿主元数据并造成假红灯 | `.gitignore` 与 artifact inventory 规则不一致 | `tools/run.py` 仅忽略普通非 symlink `.DS_Store` 并 warning；未知文件仍 hard fail | 已自动化并有专项回归 | 文件本身可在后续获批物理清理中删除 |
| 当前状态 owner 过期 | T2.35 产品判词已存在，`CURRENT-STATE.md` 仍写 `unknown` | 产品判词写回与状态 owner 同步没有同一 closeout gate | 单一 owner 和派生入口已建立 | 未自动检查业务语义同步 | 执行现场立即同步产品判词、停止点和下一任务授权边界；审计随后复核 |
| historical tests 污染默认健康信号 | 历史 T2.26 失败曾让全量信号长期发红 | active 与历史回放未分开 | `tests/entry.py` 默认只运行 active；历史显式选择 | 已自动化且 inventory fail closed | 每新增测试必须明确归类 |
| 历史正文挤占默认入口 | 原 README 266 行，多数为 2026-07-22 旧状态 | 交接快照与当前导航长期混写 | 根 README 缩成导航；旧正文由本地 Git 完整保留 | 目录测试自动核对入口长度 | 后续状态只写 `CURRENT-STATE.md` |

## 防线强度解释

- **自动防线**：错误发生时工具或测试直接报告，优先级最高。
- **流程防线**：任务卡和审计会检查，但仍依赖执行者遵守。
- **记录防线**：只留下事故事实，不能防止复发。

当前最需要补强的是状态同步。它不能由审计线程代写，因为产品判词文件、微调版本和重新冻结
应由同一执行现场闭合；审计线程负责发现不一致并阻止错误地启动下一阶段。

现有模板对所有来源审核禁用“任何浏览器自动化”，约束范围大于已经证实的 Chrome remote
debugging 故障。本轮不贸然放宽，以免重新引入 49 分钟事故；下一次确有只能交互取得的来源
时，必须先提交受控例外、10 分钟停止线和替代路线，由审计判断能力收益是否值得风险。

## 停止与恢复规则

1. 无价值阻塞超过 10 分钟：停止当前工具路线，保留错误和墙钟证据，改用已批准的低故障入口。
2. 同类事故第二次出现：不得只绕过；必须检查所有阶段的同类调用并增加自动防线或说明为何无法自动化。
3. 正式 artifact、freeze 或产品判词可能受损：立即停止写入，先复算身份；无法证明不变则视为新版本。
4. 状态 owner 与真实证据冲突：允许继续已在进行的同范围微调，但不得据此派发新的下游阶段。
5. 恢复完成后：运行最小真实回归，并由独立审计关闭问题；执行者自报不等于关闭。
