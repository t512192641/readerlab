# T2.26 使用摘要（无效运行收口）

- terminal: `INVALID_RUN`
- 发现角色调用：0
- 独立比较角色调用：0
- 候选池广度诊断调用：0
- 其他语义／评价生产角色调用：0
- 生产小组长协调模型 turn：2；平台未提供 invocation ID、服务端 token、可见文本 token、输入缓存 token、输出 token、隐藏推理 token 或精确模型工作时间，均为 `unknown` / JSON `null`。
- 语义／评价调用子集：0 / 24；重试：0。
- A、B、C 分支成本：均无语义／评价调用；C 未启动。
- 实际墙钟仅能从 run-start 到控制层 closeout 观察到；不把它冒充模型工作时间。

本文件不包含 token 估算。
