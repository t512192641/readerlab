# T2.26 v07 P1 调用与时间摘要

- 检查点：P1。
- A+B 语义／评价调用：3 / 24 上限；无重试。
- 角色：发现 Expert 1 次、独立比较者 1 次、候选池广度旁路 1 次。
- 模型／reasoning：全部为 `gpt-5.6-terra / medium`。
- 组员报告累计工作时长：约 13 分钟；A+B 120 分钟上限内。
- 生产小组长已记录协调调用：1 次；服务端调用 ID、token、工具耗时和精确墙钟均为 `unknown`，未估算。
- 服务端 token：`unknown`；可见文本 token：`unknown`；工具耗时：`unknown`。
- 产品 P1 等待：尚未开始计时，不计入模型生产时间。
- 所有实际已知调用均各有一条 `control/usage-ledger.jsonl` 记录；调用类别与 A+B 子集分开标记。
