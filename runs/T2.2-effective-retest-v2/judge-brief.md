---
status: frozen
scope: run-only
---

# 图书线裁判资格考试简报 v2

## 冻结绑定

- `contracts/T1.8-independent-acceptance.md` SHA-256：`7d8b60170669637ca7aa004e27df6fb301739d1a7009bc6f59e22dd2a48f5093`
- `runs/T2.2-effective-retest-v2/packet.md` SHA-256：`e09ce22554cfc693b358348ab85eca794023a9b6df9e6f6e5b339c31668869d0`

## 独立判断

按盲包顺序逐项判断。每一项的比较对象都是：删掉候选陪读后，读者是否会失去原文没有直接给出的具体理解或判断能力。文字正确、信息丰富、看起来专业，本身不能证明该候选值得保留。

五项必须分别判断，不得合并成总分，不得由一项代替另一项：

1. 内容增量：判断候选是否让读者得到原文没有直接给出的具体认识，而不只是复述、整理、换名、同义改写或通用提醒。只使用 `有实质增量`、`无实质增量` 或 `unknown`。
2. 解释深度：判断候选是否讲清承重判断如何发生、为何成立、在什么条件和边界下成立，而不只是给结论、列概念或堆信息。只使用 `解释充分`、`解释不足` 或 `unknown`。
3. 原文关联：判断候选是否准确回应给出的原文片段并帮助理解该处，而不是脱离原文自说自话、误读作者或把无关知识贴在附近。只使用 `关联成立`、`关联不成立` 或 `unknown`。
4. 认知杠杆：判断读者是否获得具体、可复用的理解或判断抓手，能够更好地解释、辨别、预测、权衡或迁移。理由必须说明删去候选后读者会失去或不会失去哪种能力。只使用 `形成认知杠杆`、`未形成认知杠杆` 或 `unknown`。
5. 呈现质量：判断表达是否让承重内容清楚、连贯、可读，并与原文附近的阅读过程相容。表达顺畅不能挽救没有价值的内容。只使用 `呈现清楚`、`呈现妨碍理解` 或 `unknown`。

每项判断都要说明对象内证据、对应原文证据、理由与证据边界。固定证据不足时写 `unknown`，不得扩大读取。

## 输出格式

每个 neutral ID 使用一个二级标题，并严格写出以下六行：

- `内容增量：<定性判断>｜<证据、理由与边界>`
- `解释深度：<定性判断>｜<证据、理由与边界>`
- `原文关联：<定性判断>｜<证据、理由与边界>`
- `认知杠杆：<定性判断>｜<删去后失去或不失去的具体能力、理由与边界>`
- `呈现质量：<定性判断>｜<证据、理由与边界>`
- `最终处置：<放行、拦截或边缘>`

最终处置只能是 `放行`、`拦截` 或 `边缘`。不得推测产品判词，不得把本次处置写成长期聚合合同。
