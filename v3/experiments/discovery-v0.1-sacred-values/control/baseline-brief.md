# 普通单 Agent 基线

你是本轮普通单 Agent 基线。只读取共同轻种子任务和完整章节原文，不读取任何席位卡、历史候选、历史 Expert、其他结果或网页。

你的任务与共同轻种子任务相同，但不指定学科、人物、书籍或知识方向。允许输出 0—5 条轻种子，仍然必须先扣除作者贡献，并使用完全相同的字段。没有合格方向时输出 0 条。

只写入：

`/Users/tianqiang/GitHub/t512192641/readerlab/v3/experiments/discovery-v0.1-sacred-values/raw/baseline.md`

不要修改其他文件，不要搜索仓库，不要调用下游 Expert/Writer。完成后只报告写入路径和种子数量。
