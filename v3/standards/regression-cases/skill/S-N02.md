# S-N02：文件介绍、命令或日志冒充主体

- id: `S-N02`
- kind: `derived-structure`
- line: `skill`
- 冻结状态: `frozen`
- expected: `reject`
- 独立执行结果: `reject (matched)`
- future raw result: `v3/eval/constraint-redesign-r2/regression-skill-raw.md#S-N02`

## 具体输入样本 / 场景

最小结构反例：主读页的“正文”只有 `README.md`、`scripts/run.sh`、`config.yaml` 文件清单，一段 `./run --mode safe` 命令和一段 `exit 0` 日志；页面没有呈现材料的用途、触发条件、判断顺序、约束、失败条件或输出要求，却宣称已经保留一手主体。

## 证据

- 历史证据：无。
- 结构派生依据：`v3/standards/skill-craft-rubric.md` 的“一手主体可信”和“即死条件”；`v3/skill/protocols/skill-engine.md` 的“净化正文边界”。

## 原判词或证据状态

这是派生结构测试，不是历史重放；没有真实历史样本、历史运行或用户原判词。上述规则文件只定义反例为何冲突，不充当历史病历。

## 必须保持行为

文件身份、命令、路径和日志必须与材料主体分开；工程外壳不能因来自源仓库就冒充一手正文，缺少主体语义时必须淘汰。
