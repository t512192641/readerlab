# S-N04：只列 happy path

- id: `S-N04`
- kind: `derived-structure`
- line: `skill`
- 冻结状态: `frozen`
- expected: `reject`
- 独立执行结果: `reject (matched)`
- future raw result: `v3/eval/constraint-redesign-r2/regression-skill-raw.md#S-N04`

## 具体输入样本 / 场景

最小结构反例：技术负责人页只写“接收需求 → 读取仓库 → 生成草稿 → 扫描 → 发布 issue → 派生 Agent”的成功流程。源材料实际还包含权限不足、工作区有未提交修改、用户拒绝发布、扫描失败、无法安全继续时停止与用户接管，但页面全部省略。

## 证据

- 历史证据：无。
- 结构派生依据：`v3/standards/skill-craft-rubric.md` 的“机制走查完整”和“复现测试”；`v3/skill/protocols/skill-engine.md` 的技术负责人页职责与失败条件。

## 原判词或证据状态

这是派生结构测试，不是历史重放；没有真实历史样本、历史运行或用户原判词。规则文件只提供结构预期，不被改写为既有用户判词。

## 必须保持行为

按材料适用性保留状态、权限、人工门、失败、恢复、停止和接管者；仅有成功路径时必须淘汰，不能以“流程已经讲清”放行。
