# Rejected / Downgraded

- Rejected: replacing cleaned body with an AI capability summary.
- Rejected: asset cards that require a future Agent to read the original source before taking the first action.
- Rejected: bypassing a blocking gate with `accept` or `limited_accept`.
- Downgraded: any claim of production readiness or complete GSTACK validation; this fixture is machine smoke only.
